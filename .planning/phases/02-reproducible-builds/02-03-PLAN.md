---
phase: 02-reproducible-builds
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.backend
  - .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.frontend
  - .claude/skills/comsa-k8s-deploy/SKILL.md
  - .claude/skills/comsa-k8s-deploy/references/deployment-guide.md
  - tests/test_dockerfile_pinning.R
autonomous: true
requirements: ["#123"]

must_haves:
  truths:
    - "There is exactly one Dockerfile per service in the repository, and it is the one CI builds"
    - "The k8s-deploy skill and its deployment guide point at backend/Dockerfile and frontend/Dockerfile, with no instruction to copy a template over them"
    - "No tracked file outside .planning/ still references the deleted assets/dockerfiles path"
    - "A CI test fails if a second copy of either Dockerfile is ever committed again"
  artifacts:
    - path: .claude/skills/comsa-k8s-deploy/SKILL.md
      provides: "setup instructions naming the repository Dockerfiles as the single source of truth, plus a v1.18 history entry recording the removal"
      contains: "v1.18"
    - path: .claude/skills/comsa-k8s-deploy/references/deployment-guide.md
      provides: "the same correction in the long-form guide"
    - path: tests/test_dockerfile_pinning.R
      provides: "an appended section asserting no duplicate Dockerfile and no dangling reference"
  key_links:
    - from: tests/test_dockerfile_pinning.R
      to: "git ls-files"
      via: "enumerating every tracked Dockerfile and comparing to the allowed set"
      pattern: "ls-files"
---

<objective>
Delete the drifted Dockerfile templates carried inside the `comsa-k8s-deploy` skill, repoint the
skill and its deployment guide at the real files, and add a CI assertion so a second copy cannot
come back.

Purpose: two copies is the root cause, not a thing to be managed. `assets/dockerfiles/Dockerfile.backend`
has drifted so far from `backend/Dockerfile` that copying it over the real one — which is exactly
what the skill's own quick-setup block instructs — reintroduces already-fixed bugs. This phase
pins `backend/Dockerfile`; leaving an unpinned near-duplicate one `cp` away would undo the phase
for the first person who follows the documented setup path. CLAUDE.md: delete or archive legacy
files when creating a new version.

Output: two deleted files, two corrected documents, and one appended test section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reproducible-builds/02-CONTEXT.md
@CLAUDE.md
@.claude/skills/comsa-k8s-deploy/SKILL.md
@.claude/skills/comsa-k8s-deploy/references/deployment-guide.md
@backend/Dockerfile
@frontend/Dockerfile

<interfaces>
Drift measured on 2026-08-25 by diffing the templates against the real files. Use these findings
in the documentation you write; do not re-derive them.

`assets/dockerfiles/Dockerfile.backend` vs `backend/Dockerfile` — the template is missing:
- `pool`, `jose` and `knitr` from the `install.packages()` lists
- `options(warn=2)` on every install call, so a failed install would not fail the build
- the entire Stan model recompilation step (`seqcalib.stan`, `seqcalib_mmat.stan`)
- the non-root user block (`groupadd` / `useradd` / `chown` / `USER appuser`), so an image built
  from the template runs as root
- the `cmake` rationale comment added in PR #122

`assets/dockerfiles/Dockerfile.frontend` vs `frontend/Dockerfile` — the template is missing:
- the cache-busting fix (real file line 21 is `RUN rm -rf dist/ && npm run build`; template line
  19 is `RUN npm run build`)
- the "Trigger build for GitHub Container Registry" header comment
So the frontend template has the same duplication failure mode and is handled the same way.

Every tracked reference to the templates, from `grep -rn` excluding `.planning/`:
- `.claude/skills/comsa-k8s-deploy/SKILL.md` lines 175-176 — the two `cp assets/dockerfiles/...`
  lines inside the "Quick Setup" numbered step 1 code block
- `.claude/skills/comsa-k8s-deploy/SKILL.md` line 346 — the `dockerfiles/` bullet in the "Assets"
  list
- `.claude/skills/comsa-k8s-deploy/references/deployment-guide.md` lines 30-31 — the same two
  `cp` lines
- `.planning/codebase/CONCERNS.md` line 139 mentions the template path in a findings document.
  Leave it alone; codebase maps are dated snapshots, not live instructions.

`SKILL.md` line 208 and `deployment-guide.md` line 85
(`git add k8s/ backend/Dockerfile frontend/Dockerfile ...`) stay exactly as they are — those
paths are still correct.

The Version History section of `SKILL.md` starts at line 21 with `### v1.16 - 2026-01-20`,
followed by `### v1.17 - 2026-01-20`. Existing entries are not in strict version order.

Also observed and deliberately NOT in scope: `assets/.github/workflows/deploy.yml` is a third
copied-and-drifted asset with the same problem. CONTEXT scopes this phase to the Dockerfile
copies only. Record it in the SUMMARY; do not change it.

`tests/test_dockerfile_pinning.R` is created by plan 02-01 and already has the `test()` and
`section()` helpers and the trailing summary/`quit()` block. Append a new section immediately
before that summary block; do not restructure what is there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete both templates and repoint the skill and the deployment guide</name>

  <files>.claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.backend, .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.frontend, .claude/skills/comsa-k8s-deploy/SKILL.md, .claude/skills/comsa-k8s-deploy/references/deployment-guide.md</files>

  <read_first>
    - .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.backend (confirm the drift yourself before deleting — do not delete on the strength of this plan's description alone)
    - .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.frontend (same)
    - backend/Dockerfile and frontend/Dockerfile (the surviving originals)
    - .claude/skills/comsa-k8s-deploy/SKILL.md (Version History from line 21, Quick Setup at lines 170-213, Assets list at lines 344-347)
    - .claude/skills/comsa-k8s-deploy/references/deployment-guide.md (the setup section around lines 20-90)
  </read_first>

  <action>
    Delete both template files with `git rm`:
    `.claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.backend` and
    `.claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.frontend`. The
    `assets/dockerfiles/` directory then holds nothing; confirm it is gone from the working tree
    and `rmdir` it if the filesystem left it behind.

    In `SKILL.md`, inside the "Quick Setup" numbered step 1 code block, remove the two lines
    `cp assets/dockerfiles/Dockerfile.backend backend/Dockerfile` and
    `cp assets/dockerfiles/Dockerfile.frontend frontend/Dockerfile` together with their
    `# Dockerfiles` comment. Put a short prose line above the code block in their place stating
    that `backend/Dockerfile` and `frontend/Dockerfile` live in the repository and are the single
    source of truth — there is no template to copy — and that the backend one is deliberately
    pinned (base-image digest plus a dated CRAN snapshot), so copying an unpinned variant over it
    would silently undo issue #123.

    In `SKILL.md`, in the "Assets" list, delete the `dockerfiles/` bullet. Leave the `k8s/*.yaml`
    and `.github/workflows/deploy.yml` bullets alone.

    In `SKILL.md`, insert a new entry as the FIRST entry under `## Version History`, above
    `### v1.16 - 2026-01-20`, headed `### v1.18 - 2026-08-25`. It must record: what was removed
    (both Dockerfile templates under `assets/dockerfiles/`); why (drift — the backend template was
    missing `pool`, `jose`, `knitr`, `options(warn=2)`, the entire Stan model recompilation step
    and the non-root user block, so an image built from it would run as root with a package set
    that cannot run the calibration pipeline, and the frontend template was missing the
    `rm -rf dist/` cache-busting fix); the impact (the skill now points at the repository's own
    Dockerfiles, so a `cp` from a template can no longer silently regress a deploy, and the
    pinning added for issue #123 cannot be reverted by following the setup instructions); and the
    files touched. Follow the shape of the surrounding entries — bolded lead word, bullet list,
    a `**Files:**` line.

    In `references/deployment-guide.md`, remove the two
    `cp .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.*` lines and replace them
    with the same statement: the Dockerfiles already exist in the repository at
    `backend/Dockerfile` and `frontend/Dockerfile` and must not be overwritten from a template.

    Do not touch `assets/k8s/*`, `assets/.github/workflows/deploy.yml`, the `scripts/` directory,
    `references/k8s-cluster-info.md`, or `.planning/codebase/CONCERNS.md`. Do not change
    `SKILL.md` line 208 or `deployment-guide.md` line 85.
  </action>

  <verify>
    <automated>cd /Users/ericliu/projects5/comsa_dashboard; test ! -e .claude/skills/comsa-k8s-deploy/assets/dockerfiles &amp;&amp; test -z "$(git ls-files .claude/skills/comsa-k8s-deploy/assets/dockerfiles)" &amp;&amp; test -z "$(git grep -l 'assets/dockerfiles' -- . ':!.planning' ':!tests')" &amp;&amp; grep -q 'v1.18' .claude/skills/comsa-k8s-deploy/SKILL.md &amp;&amp; echo ok</automated>
  </verify>

  <acceptance_criteria>
    - The verify command prints `ok` and exits 0.
    - `git status --porcelain .claude/skills/comsa-k8s-deploy/assets/dockerfiles` shows two `D`
      entries (both templates deleted), and no other file under `assets/` is modified:
      `git status --porcelain .claude/skills/comsa-k8s-deploy/assets | grep -v dockerfiles` prints
      nothing.
    - `grep -c 'cp assets/dockerfiles' .claude/skills/comsa-k8s-deploy/SKILL.md` returns `0`.
    - `grep -c 'assets/dockerfiles' .claude/skills/comsa-k8s-deploy/references/deployment-guide.md`
      returns `0`.
    - `grep -c 'git add k8s/ backend/Dockerfile frontend/Dockerfile' .claude/skills/comsa-k8s-deploy/SKILL.md`
      returns `1` — the still-correct `git add` line was not collaterally edited.
    - `sed -n '/^## Version History/,/^### v1.16/p' .claude/skills/comsa-k8s-deploy/SKILL.md | grep -c '### v1.18 - 2026-08-25'`
      returns `1` — the new entry is the first one in the section.
    - The v1.18 entry names all five backend drift items and the frontend drift item:
      `sed -n '/### v1.18/,/### v1.16/p' .claude/skills/comsa-k8s-deploy/SKILL.md | grep -c -e pool -e jose -e knitr -e 'warn=2' -e Stan -e 'rm -rf dist'`
      returns at least `6`.
    - `git ls-files -- '*/Dockerfile*' 'Dockerfile*' | sort` prints exactly
      `backend/Dockerfile` and `frontend/Dockerfile`, nothing else.
  </acceptance_criteria>

  <done>
    Both templates are gone, the skill and the guide instruct the reader to use the repository's
    own Dockerfiles, and no tracked file outside `.planning/` still points at the deleted path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assert single-copy Dockerfiles from CI</name>

  <files>tests/test_dockerfile_pinning.R</files>

  <read_first>
    - tests/test_dockerfile_pinning.R (created by plan 02-01 — reuse its `test()` / `section()` helpers and append before its summary block)
    - .claude/skills/comsa-k8s-deploy/SKILL.md (as edited in Task 1, to confirm the references are gone)
  </read_first>

  <action>
    Append one new `section()` to `tests/test_dockerfile_pinning.R`, headed along the lines of
    "exactly one Dockerfile per service", placed after the existing sections and before the
    `Total: / Passed: / Failed:` summary block. Three assertions:

    1. The path `.claude/skills/comsa-k8s-deploy/assets/dockerfiles` does not exist
       (`!file.exists(...)`), resolved relative to the project root the same way the existing
       Dockerfile path probing works.

    2. The set of tracked Dockerfiles is exactly `backend/Dockerfile` and `frontend/Dockerfile`.
       Enumerate with `system2("git", c("ls-files"), stdout = TRUE)` and keep entries whose
       `basename()` starts with `Dockerfile` (case-sensitive, `^Dockerfile`). Using `git ls-files`
       rather than `list.files()` is deliberate: it ignores `node_modules` and anything untracked.
       Compare with `identical(sort(found), c("backend/Dockerfile", "frontend/Dockerfile"))` so
       both a missing file and an extra copy fail. If `git` is unavailable the `system2` call
       returns nothing — guard against that by failing the assertion with a clear message rather
       than passing on an empty result.

    3. No tracked file still points at the deleted template path. Run
       `git grep -l -- "assets/dockerfiles"` and require the result to be empty after excluding
       `.planning/` (dated planning documents legitimately discuss the path) and this test file
       itself (it necessarily contains the literal string). Implement the exclusion by filtering
       the returned paths in R, not by relying on git pathspecs, so the intent is readable.
       Note that `git grep -l` exits 1 when there are no matches, so call it with
       `system2(..., stdout = TRUE, stderr = FALSE)` and treat a zero-length result as a pass.

    Do not modify any assertion written by plan 02-01, and do not change the summary/`quit()`
    block.
  </action>

  <verify>
    <automated>cd /Users/ericliu/projects5/comsa_dashboard; Rscript tests/test_dockerfile_pinning.R</automated>
  </verify>

  <acceptance_criteria>
    - `Rscript tests/test_dockerfile_pinning.R` exits 0 and its summary reports `Failed: 0`.
    - The new section genuinely fails when violated. Prove it, then undo it:
      `mkdir -p .claude/skills/comsa-k8s-deploy/assets/dockerfiles && cp backend/Dockerfile .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.backend && git add -N .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.backend && Rscript tests/test_dockerfile_pinning.R; echo "exit=$?"`
      must print `exit=1` with FAIL lines for both the directory-absence and the
      tracked-Dockerfile-set assertions. Then
      `git rm -f --cached .claude/skills/comsa-k8s-deploy/assets/dockerfiles/Dockerfile.backend; rm -rf .claude/skills/comsa-k8s-deploy/assets/dockerfiles`
      and confirm `Rscript tests/test_dockerfile_pinning.R` exits 0 again and
      `git status --porcelain` is clean apart from this plan's intended changes.
    - `grep -c 'requireNamespace\|library(' tests/test_dockerfile_pinning.R` still returns `0` —
      the test remains base-R only so it can run before `Install jsonlite` in CI.
    - The total assertion count grew by exactly 3. Measure the delta yourself rather than
      relying on a number recorded elsewhere: run `Rscript tests/test_dockerfile_pinning.R`
      and note the `Total:` value BEFORE editing the file, then again AFTER, and confirm
      `after - before == 3`.
  </acceptance_criteria>

  <done>
    Committing a second copy of either Dockerfile, or leaving a document pointing at the deleted
    template path, fails `Rscript tests/test_dockerfile_pinning.R` and therefore fails CI.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill asset → repository file | A documented `cp` lets an asset overwrite a production build definition |
| documentation → operator action | An operator following stale setup instructions changes what ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | `assets/dockerfiles/Dockerfile.backend` overwriting `backend/Dockerfile` via the skill's own documented `cp` | mitigate | The template is deleted rather than synced, and `tests/test_dockerfile_pinning.R` fails if any Dockerfile other than `backend/Dockerfile` and `frontend/Dockerfile` is ever tracked again. Without this, following the documented setup would revert the digest pin, the snapshot pin and the explicit `sodium` install in one command. |
| T-02-11 | Elevation of Privilege | The deleted backend template had no `USER appuser` block, so an image built from it runs as root | mitigate | Deleting the template removes the only in-repo path to a root-running backend image. The `k8s/backend-deployment.yaml` pod security context (`runAsNonRoot`) is a second layer, but it would have turned this into a CrashLoopBackOff rather than a caught mistake. |
| T-02-12 | Repudiation | Documentation pointing at a nonexistent path — a `cp` that silently no-ops leaves whatever Dockerfile was already present and the operator believes they applied the template | mitigate | The `cp` lines are removed rather than left dangling, and a CI assertion fails if any tracked file outside `.planning/` still references `assets/dockerfiles`. |
| T-02-13 | Tampering | `assets/.github/workflows/deploy.yml`, a third drifted copied asset with the same failure mode | accept | Out of scope for this phase per CONTEXT, which bounds the work to the Dockerfile copies. It is recorded in the SUMMARY so it is not lost. It cannot revert the pinning: it copies a workflow, not a Dockerfile. |
</threat_model>

<verification>
- `Rscript tests/test_dockerfile_pinning.R` exits 0 and reports `Failed: 0`.
- The negative test in Task 2's acceptance criteria exits 1, then exits 0 again after cleanup.
- `git ls-files -- '*/Dockerfile*' 'Dockerfile*'` lists exactly two paths.
- `git grep -l 'assets/dockerfiles' -- . ':!.planning' ':!tests'` returns nothing.
- `SKILL.md` and `deployment-guide.md` still contain their unchanged
  `git add k8s/ backend/Dockerfile frontend/Dockerfile` lines.
</verification>

<success_criteria>
1. `.claude/skills/comsa-k8s-deploy/assets/dockerfiles/` and both files in it no longer exist and
   are no longer tracked.
2. `SKILL.md` Quick Setup and Assets list, and `references/deployment-guide.md`, name
   `backend/Dockerfile` and `frontend/Dockerfile` as the single source of truth with no `cp` from
   a template.
3. `SKILL.md` carries a `### v1.18 - 2026-08-25` entry as the first Version History item,
   recording the drift that motivated the deletion.
4. `tests/test_dockerfile_pinning.R` fails if a second Dockerfile copy is committed or a dangling
   reference remains.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reproducible-builds/02-03-SUMMARY.md`.
Record the out-of-scope observation about `assets/.github/workflows/deploy.yml` being a third
drifted copy, and the new `Total:` assertion count for `tests/test_dockerfile_pinning.R`.
</output>
