---
phase: 02.1-adopt-vacalibration-2-3-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Dockerfile
  - backend/package-manifest.csv
  - tests/test_dockerfile_pinning.R
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "backend/Dockerfile installs vacalibration from GitHub at the full 40-character commit 498df45b6e14e02a3152b843c605b4a33240f12e, and no install.packages() call in the Dockerfile names that package any more (ROADMAP criterion 1, D-01)"
    - "backend/package-manifest.csv records vacalibration,2.3.1; every other row is byte-identical to the pre-phase file (post-review: the remotes row was dropped with the switch to a tarball install) (ROADMAP criterion 1, D-02)"
    - "ENV CRAN_SNAPSHOT is still https://p3m.dev/cran/__linux__/noble/2026-08-01 and openVA, EAVA and knitr still come from that snapshot (ROADMAP criterion 2, D-05)"
    - "The Stan compile step still compiles seqcalib.stan and seqcalib_mmat.stan from source and writes both .rds files itself, so the removal of the bundled seqcalib.rds in 2.3.1 changes nothing (ROADMAP criterion 2, D-04)"
    - "Rscript tests/test_dockerfile_pinning.R exits 0 on the pinned Dockerfile and exits 1 when the ref is replaced by a branch name (ROADMAP criterion 1, D-03)"
    - "A real vacalibration 2.3.1 run on frontend/public/sample_eava_child.csv through build_broad_matrix()/build_donotcalib() keeps malaria calibrated, returns a full 1x9x9 Mmat_tomodel and a non-stalled lambda (ROADMAP criterion 3, D-10)"
  artifacts:
    - "backend/Dockerfile — ENV VACALIBRATION_SHA beside CRAN_SNAPSHOT, and a RUN step that install.packages() the GitHub archive tarball of that SHA with repos = NULL (post-review: replaces remotes::install_github, which is anonymous-API-rate-limited on shared runner IPs)"
    - "backend/package-manifest.csv — the row vacalibration,2.3.1 (no other row changes)"
    - "tests/test_dockerfile_pinning.R — a new section asserting the vacalibration ref is a 40-hex commit SHA, plus a VACAL_SHA constant beside BASE_DIGEST/SNAPSHOT_URL"
  key_links:
    - "Dockerfile SHA pin <-> backend/package-manifest.csv vacalibration row <-> the deploy.yml step 'Verify backend package manifest (issue #123)', which fails the deploy if the two disagree (D-02, D-12)"
    - "The Stan compile step's find.package('vacalibration')/stan directory <-> the GitHub-installed package layout (inst/stan becomes <pkg>/stan on install, so the step needs no edit)"
    - "The tarball install with repos = NULL touches no dependency <-> every 2.3.1 Import is already on the manifest from CRAN_SNAPSHOT, which is what lets the first manifest diff pass with only the vacalibration row changed"
  prohibitions:
    - statement: "CRAN_SNAPSHOT must not move off 2026-08-01 in this phase"
      status: enforced
      verification: "tests/test_dockerfile_pinning.R asserts the snapshot URL occurs exactly once in the comment-stripped Dockerfile; the SNAPSHOT_URL constant is unchanged"
    - statement: "The vacalibration ref must never be a branch or a tag"
      status: enforced
      verification: "the new section's 40-hex assertion, plus the mutation check in Task 2's verify"
    - statement: "No manifest row other than vacalibration may change"
      status: enforced
      verification: "git diff --numstat 30e082d2e6906d3c062f29fd5d2889fe463d90f2 -- backend/package-manifest.csv reports exactly 2 added and 1 deleted line"
    - statement: "knitr must not be dropped from the image (D-05)"
      status: enforced
      verification: "the CRAN install line still names knitr and the manifest keeps knitr,1.51"
    - statement: "No dependency may be upgraded by the GitHub install"
      status: enforced
      verification: "repos = NULL on the tarball install (no index to resolve upgrades against; the pinning guard asserts it), plus the deploy.yml manifest diff in plan 02.1-03"
---

<objective>
Pin the backend image to vacalibration 2.3.1 at GitHub commit `498df45b6e14e02a3152b843c605b4a33240f12e`,
keep every other package on the 2026-08-01 CRAN snapshot, and prove the pinned commit actually
calibrates through this repository's own helper path before anything else in the phase is touched.

Purpose: StanHeaders 2.39.1 (CRAN, 2026-09-02) does not compile vacalibration 2.2's Stan models, so
the Phase 2 snapshot pin is the only thing keeping the image building and cannot move until this
lands. This plan is the tracer: one thin path from the pinned install through `build_broad_matrix()`,
`build_donotcalib()` and `not_calibrated_causes()` to a real calibrated result.

Output: a pinned `backend/Dockerfile`, an agreeing `backend/package-manifest.csv`, a CI guard that
refuses a floating ref, and a recorded end-to-end run on the issue #101 sample.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-CONTEXT.md
@.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-PATTERNS.md
@.planning/notes/vacalibration-2-3-1-vs-phase-1.md
@.planning/seeds/switch-vacalibration-pin-to-cran.md
@.planning/phases/02-reproducible-builds/02-01-SUMMARY.md
@CLAUDE.md
</context>

<verified_facts>
Checked against primary sources on 2026-09-12. Use these values verbatim; do not re-derive them.

- Full commit SHA for vacalibration 2.3.1: `498df45b6e14e02a3152b843c605b4a33240f12e`
  (GitHub API `/repos/sandy-pramanik/vacalibration/commits/498df45`, authored 2026-09-09).
- `DESCRIPTION` at that commit reads `Version: 2.3.1` and
  `Imports: rstan (>= 2.26), openVA, parallel, ggplot2, patchwork, reshape2, LaplacesDemon, MASS` —
  the same import set as CRAN 2.2, so no new dependency enters the image.
- The pinned snapshot (`https://p3m.dev/cran/__linux__/noble/2026-08-01/src/contrib/PACKAGES.gz`)
  serves `remotes 2.5.0`. `remotes` imports only base packages, so it adds exactly one manifest row.
- Manifest ordering is `LC_COLLATE=C`; the neighbours are `rematch,2.0.0` (line 105) and
  `reshape2,1.4.5` (line 106), so `remotes,2.5.0` belongs between them.
- The package at that commit has no `src/` directory — the install is pure R plus `inst/stan` — and
  it still ships `data/comsamoz_CCVAoutput.rda`, the dataset `tests/test_vacalibration_backend.R`
  section 2b needs and which the local vacalibration 2.0 install lacks.
- Local R already has `remotes 2.5.0`, `rstan 2.32.7`, `StanHeaders 2.32.10`, `openVA 1.2.0`,
  `knitr 1.51`; `LaplacesDemon` is ABSENT locally and will be installed as a missing dependency.
</verified_facts>

<tasks>

<task type="tracer">
  <name>Task 1: Pin vacalibration 2.3.1 and prove one real calibration end to end</name>
  <files>backend/Dockerfile, backend/package-manifest.csv</files>
  <read_first>
    - `backend/Dockerfile` lines 48-119 — the `ENV CRAN_SNAPSHOT` block, the three `options(warn=2); install.packages(...)` lines (68-77), the Stan compile step (82-105), the manifest emission step (113-119)
    - `backend/package-manifest.csv` lines 100-135 — the C-collated neighbourhood the new row lands in, and the current `vacalibration,2.2` row (line 129)
    - `.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-PATTERNS.md` section "backend/Dockerfile" — the `options(warn=2)` precedent and why the manifest emission step needs no edit
    - `.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-CONTEXT.md` decisions D-01, D-02, D-04, D-05
    - `backend/jobs/utils.R` lines 735-805 (`zero_count_causes`, `build_donotcalib`, `prepare_calibration_exclusions`) and lines 1104-1122 (`not_calibrated_causes`) — the helper path the end-to-end proof drives
  </read_first>
  <action>
    Install the pinned commit into the local R library first, because every later verification in
    this phase runs against it: `remotes::install_github("sandy-pramanik/vacalibration@498df45b6e14e02a3152b843c605b4a33240f12e", upgrade = "never")`.
    `upgrade = "never"` is load-bearing, not cosmetic — local `rstan 2.32.7` and `StanHeaders 2.32.10`
    must stay identical to the image manifest, and an upgrade to StanHeaders 2.39.x would destroy the
    compile parity this phase rests on. `LaplacesDemon` is the one missing dependency and will be
    installed; nothing else should move.

    Then edit `backend/Dockerfile`, keeping every convention already established in the file
    (per D-01, D-04, D-05):

    1. Remove the package name from the existing CRAN install on line 77 so that line installs only
       `openVA`, `EAVA` and `knitr` from the snapshot. Leave its `options(warn=2)` preamble intact.
    2. Rewrite the comment block above it (lines 74-76). EAVA's explanation stays. The knitr line
       must stop asserting a runtime need "in v2.2"; record instead that knitr is still Suggests-only
       at 2.3.1, that the runtime need was observed rather than documented, and that it is therefore
       still installed.
    3. Add a new `RUN` step immediately after it and before the Stan compile step that, under
       `options(warn=2)`, installs `remotes` from the snapshot, calls `remotes::install_github` on
       `sandy-pramanik/vacalibration` at the full 40-character SHA
       `498df45b6e14e02a3152b843c605b4a33240f12e` with `upgrade = "never"`, and ends with a
       `stopifnot()` that the installed `packageVersion("vacalibration")` equals `2.3.1`, so a pin
       that silently resolves to anything else fails the build instead of shipping. Add a short
       comment naming why this one package is off the snapshot (CRAN still carries 2.2; StanHeaders
       2.39.1 breaks 2.2's Stan models) and pointing at
       `.planning/seeds/switch-vacalibration-pin-to-cran.md` as the reversal path.
    4. Change nothing else. The Stan compile step stays exactly as it is (D-04: it compiles both
       models from the installed `stan` directory and writes both `.rds` files itself, so 2.3.1
       dropping the bundled `seqcalib.rds` is a non-event). `ENV CRAN_SNAPSHOT` stays at the
       2026-08-01 URL. The `required` vector in the manifest emission step already contains the
       package name and needs no edit.

    Then edit `backend/package-manifest.csv`: set the `vacalibration` row to version `2.3.1` and
    insert the row `remotes,2.5.0` between `rematch,2.0.0` and `reshape2,1.4.5`. Those are the only
    two lines that may differ — D-02 says any third changed row means dependencies drifted and the
    install mechanism is wrong.

    Finally run the end-to-end proof below. It sources `backend/jobs/utils.R` directly from the
    repository root, which is the supported way to exercise backend logic on this machine (the local
    R library has neither DBI nor RPostgres, so the plumber backend cannot start here). The first
    run compiles `seqcalib.stan` lazily because 2.3.1 ships no compiled `.rds`; roughly 20-30 seconds
    of compilation plus the MCMC run is expected behaviour, not a failure.

    If the local install fails, stop and report the actual error. Do not fall back to a tag, a
    branch, or the CRAN copy.
  </action>
  <verify>
    <automated>Rscript -e 'stopifnot(identical(as.character(packageVersion("vacalibration")), "2.3.1")); cat("LOCAL 2.3.1 OK\n")'</automated>
    <fails_when>non-zero exit, or the line "LOCAL 2.3.1 OK" is absent — the local library is still on 2.0/2.2 and every later verification in this phase would be testing the wrong package</fails_when>
    <automated>Rscript -e 'library(vacalibration); source("backend/jobs/utils.R"); df <- read.csv("frontend/public/sample_eava_child.csv", stringsAsFactors = FALSE); vi <- list(eava = build_broad_matrix(df, "child")); r <- vacalibration(va_data = vi, age_group = "child", country = "Mozambique", missmat_type = "prior", ensemble = FALSE, donotcalib = build_donotcalib(vi), nMCMC = 400, nBurn = 200, nThin = 1, verbose = FALSE); cs <- colnames(r$p_uncalib); stopifnot(!("malaria" %in% not_calibrated_causes(r, "eava", cs)), identical(as.integer(dim(r$Mmat_tomodel)), c(1L, 9L, 9L)), isFALSE(path_correction_stalled(r$lambda_calibpath[[1]]))); cat("TRACER OK\n")'</automated>
    <fails_when>non-zero exit, the line "TRACER OK" is absent, or a stopifnot error naming malaria, Mmat_tomodel or path_correction_stalled appears — meaning 2.3.1 declines malaria, shrinks the misclassification matrix, or stalls path correction on the issue #101 sample</fails_when>
    <automated>test "$(grep -c 498df45b6e14e02a3152b843c605b4a33240f12e backend/Dockerfile)" -ge 1 &amp;&amp; grep -qx 'vacalibration,2.3.1' backend/package-manifest.csv &amp;&amp; grep -qx 'remotes,2.5.0' backend/package-manifest.csv &amp;&amp; grep -q 'ENV CRAN_SNAPSHOT=https://p3m.dev/cran/__linux__/noble/2026-08-01' backend/Dockerfile &amp;&amp; test "$(wc -l < backend/package-manifest.csv)" -eq 136 &amp;&amp; head -1 backend/package-manifest.csv | grep -qx 'Package,Version' &amp;&amp; diff <(tail -n +2 backend/package-manifest.csv | cut -d, -f1) <(tail -n +2 backend/package-manifest.csv | cut -d, -f1 | LC_ALL=C sort) &amp;&amp; echo PIN-OK</automated>
    <fails_when>the line "PIN-OK" is absent — the SHA is missing from the Dockerfile, a manifest row is wrong or missing, the snapshot URL moved, the manifest is not exactly 136 lines (135 before plus the one added remotes row — a different count means extra packages were installed or rows were lost), or the package column is no longer C-sorted so the deploy-time diff would fail</fails_when>
    <automated>DIFF=$(git diff --numstat 30e082d2e6906d3c062f29fd5d2889fe463d90f2 -- backend/package-manifest.csv) &amp;&amp; echo "$DIFF" &amp;&amp; test "$(echo "$DIFF" | awk '{print $1"/"$2}')" = "2/1" &amp;&amp; echo ROWS-OK</automated>
    <fails_when>the line "ROWS-OK" is absent — measured against the pre-phase commit 30e082d, the manifest gained or lost anything other than exactly 2 added and 1 deleted line, which means a dependency drifted (D-02)</fails_when>
  </verify>
  <done>
    Local R reports vacalibration 2.3.1; `backend/Dockerfile` installs it from GitHub at the full SHA
    with dependency upgrades disabled and a build-time version assertion; `openVA`, `EAVA` and
    `knitr` still come from the unchanged 2026-08-01 snapshot; the Stan compile step is untouched;
    `backend/package-manifest.csv` differs from its previous state by exactly the `vacalibration`
    version and one added `remotes,2.5.0` row; and a real calibration on the issue #101 sample keeps
    malaria calibrated with a full 1x9x9 matrix and a non-stalled lambda.
  </done>
  <reversibility rating="reversible">
    The pin reverts to `install.packages('vacalibration')` on the snapshot the moment CRAN carries
    2.3.1 — that reversal is already written up in `.planning/seeds/switch-vacalibration-pin-to-cran.md`.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Guard the pin — the ref must be a commit SHA, in CI, forever</name>
  <files>tests/test_dockerfile_pinning.R</files>
  <read_first>
    - `tests/test_dockerfile_pinning.R` lines 1-60 — the header rationale, the `test()`/`section()` helpers, the `lines` / comment-stripped `code` vectors, and the `BASE_DIGEST` / `SNAPSHOT_URL` constants
    - `tests/test_dockerfile_pinning.R` lines 81-118 — the "CRAN snapshot is pinned in exactly one place" section (the style to copy) and the "sodium is an explicit dependency" section that follows it
    - `backend/Dockerfile` — the install step written in Task 1, so the assertions match the real line
    - `.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-PATTERNS.md` section "tests/test_dockerfile_pinning.R" — the rule that pinning assertions read the comment-stripped `code` vector, never raw `lines`
    - `.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-CONTEXT.md` decision D-03
  </read_first>
  <action>
    Add a `VACAL_SHA` constant holding `498df45b6e14e02a3152b843c605b4a33240f12e` beside the existing
    `BASE_DIGEST` and `SNAPSHOT_URL` constants, then add one new `section()` titled for the commit-SHA
    pin. Place it after the CRAN-snapshot section and before the sodium section so the
    "no floating references" guards stay grouped. Reuse the existing `code` vector and the existing
    `test()` helper; do not re-read the Dockerfile and do not re-strip comments.

    Assertions, all against `code` (per D-03, existing snapshot assertions stay untouched):

    1. Exactly one line of `code` references the upstream repository path `sandy-pramanik/vacalibration`.
    2. The ref token that follows the `@` on that line matches the positive form `^[0-9a-f]{40}$`.
       State this positively rather than as a list of forbidden names: a 40-hex match is unreachable
       for a branch name or a version tag, so one assertion covers both cases D-03 names.
    3. That ref token equals `VACAL_SHA`.
    4. No `install.packages(` line in `code` still carries the package name in quotes — the package
       must not silently come back off the CRAN snapshot alongside the GitHub pin.
    5. The GitHub install line disables dependency upgrades (it carries `upgrade` set to `never`).
    6. `backend/package-manifest.csv` contains the exact row `vacalibration,2.3.1`, read with
       `readLines()` from the already-resolved repository root used by the "exactly one Dockerfile
       per service" section. This is the cheap local half of the deploy-time manifest diff: it
       catches a Dockerfile/manifest disagreement before the push instead of in the deploy job.

    Keep the suite's properties: base R only, no network, no Docker, runnable from the project root
    or from `backend/`. Do not touch the summary/exit-code block.
  </action>
  <verify>
    <automated>Rscript tests/test_dockerfile_pinning.R</automated>
    <fails_when>non-zero exit, or any line beginning with "  FAIL:" in the output</fails_when>
    <automated>cp backend/Dockerfile "${TMPDIR:-/tmp}/Dockerfile.pinguard.bak" &amp;&amp; perl -pi -e 's/498df45b6e14e02a3152b843c605b4a33240f12e/mainline-branch-ref/' backend/Dockerfile; Rscript tests/test_dockerfile_pinning.R >/dev/null 2>&amp;1; rc=$?; cp "${TMPDIR:-/tmp}/Dockerfile.pinguard.bak" backend/Dockerfile; test $rc -eq 1 &amp;&amp; cmp -s "${TMPDIR:-/tmp}/Dockerfile.pinguard.bak" backend/Dockerfile &amp;&amp; echo MUTATION-GUARD-OK</automated>
    <fails_when>the line "MUTATION-GUARD-OK" is absent — either the guard exited 0 with a non-SHA ref in place (it does not actually catch a branch name), or `cmp` reports the Dockerfile was not restored byte-identically from the backup</fails_when>
  </verify>
  <done>
    `Rscript tests/test_dockerfile_pinning.R` passes with the new section present, its assertion
    count higher than before, and it exits 1 when the ref is swapped for a branch name. The Dockerfile
    is restored byte-identical after the mutation check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRAN snapshot -> image | Pinned, dated, single-source; already guarded by Phase 2 |
| github.com -> image build | NEW in this phase: one R package now enters the image from a source outside the pinned snapshot |
| built image -> deploy gate | `deploy.yml` extracts `/opt/package-manifest.csv` and diffs it against the committed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-01 | Tampering | vacalibration install in `backend/Dockerfile` | high | mitigate | Pin to the full 40-char commit SHA `498df45b6e14e02a3152b843c605b4a33240f12e`. A git commit SHA is content-addressed, so the installed tree cannot change under the pin even if the branch or a tag is force-moved upstream. `tests/test_dockerfile_pinning.R` fails the build in CI if the ref stops being 40 hex characters. |
| T-02.1-02 | Tampering | dependency closure of the GitHub install | high | mitigate | `upgrade = "never"` so the installer cannot pull newer versions of `rstan`/`StanHeaders`/`openVA` off live CRAN; the committed manifest plus the `deploy.yml` diff turns any drifted row into a failed deploy. |
| T-02.1-03 | Spoofing | build-time resolution of the pin | medium | mitigate | A build-time `stopifnot(packageVersion("vacalibration") == "2.3.1")` in the same RUN step, so a pin that resolves to a different tree fails the build rather than shipping. |
| T-02.1-04 | Denial of Service | github.com reachability at build time | low | accept | The build already depends on network reachability for the CRAN snapshot; an unreachable GitHub fails the build loudly under `options(warn=2)`. No retry logic is added. |
| T-02.1-05 | Tampering | package legitimacy | medium | accept | The package-legitimacy gate covers npm/pip/cargo installs; this is neither. `vacalibration` is this project's existing, already-shipped core dependency, authored by the named collaborator (`sandy-pramanik`), previously installed from CRAN, and the exact commit was read and diffed against CRAN 2.2 in `.planning/notes/vacalibration-2-3-1-vs-phase-1.md`. `remotes` comes from the pinned CRAN snapshot. No `[ASSUMED]`/`[SUS]` package is introduced. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 02.1-01)

Created:
- `tests/test_dockerfile_pinning.R` — new constant `VACAL_SHA`, new `section()` asserting the
  vacalibration ref is the pinned 40-hex commit SHA, that no `install.packages(` line still names the
  package, that dependency upgrades are disabled, and that `backend/package-manifest.csv` carries
  `vacalibration,2.3.1`.

Changed:
- `backend/Dockerfile` — the CRAN install line loses one package name; its comment block is rewritten
  for D-05; a new `RUN` step installs `remotes` then `install_github` at the pinned SHA with
  `upgrade = "never"` and a build-time `stopifnot()` on the installed version.
- `backend/package-manifest.csv` — `vacalibration,2.2` becomes `vacalibration,2.3.1`; new row
  `remotes,2.5.0`.

Not changed (deliberately): `ENV CRAN_SNAPSHOT`, the Stan compile step, the manifest emission step's
`required` vector, `.github/workflows/test.yml`, `.github/workflows/deploy.yml`, and every file under
`backend/jobs/` (those belong to plan 02.1-02).
</artifacts_produced>

<verification>
1. `Rscript -e 'packageVersion("vacalibration")'` reports 2.3.1 locally.
2. The tracer command prints `TRACER OK`.
3. `Rscript tests/test_dockerfile_pinning.R` exits 0; the same command exits 1 against a Dockerfile
   whose ref has been swapped for a branch name.
4. `git diff --numstat 30e082d -- backend/package-manifest.csv` reports exactly 2 added and 1 deleted line.
5. `git diff 30e082d -- backend/Dockerfile` shows no change to `ENV CRAN_SNAPSHOT` and none to the Stan
   compile step.
</verification>

<success_criteria>
- ROADMAP criterion 1 (source half): the Dockerfile pin and the manifest row agree and are guarded.
- ROADMAP criterion 2: the snapshot is unchanged and the Stan compile step is intact.
- ROADMAP criterion 3 (local half): the issue #101 sample still calibrates with malaria calibrated.
- D-01, D-02, D-03, D-04, D-05 all implemented and verifiable by command.
</success_criteria>

<output>
Create `.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-01-SUMMARY.md` when done. Record the
observed local install output (including which dependencies `remotes` installed), the tracer run's
lambda and the excluded-cause set it reported, and the new assertion count of
`tests/test_dockerfile_pinning.R`.
</output>
