---
phase: 02.1-adopt-vacalibration-2-3-1
plan: 03
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02"]
files_modified: []
autonomous: false
requirements: [D-07, D-11, D-12]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

user_setup:
  - service: github-actions
    why: "Only a push/merge to master triggers the backend image build and the dev deploy; an executor agent cannot push or observe a run"
    dashboard_config:
      - task: "Merge the phase branch to master (repo convention is a PR) and watch the Deploy to k8s-dev run"
        location: "https://github.com/cliu238/comsa_dashboard/actions"
  - service: comsa-dashboard-dev
    why: "The dev API has been login-only since PR #112; anonymous curl returns 401 for everything except /health, so job submission and result inspection need a browser session"
    dashboard_config:
      - task: "Log in and run the two acceptance jobs through the UI"
        location: "https://dev.sites.idies.jhu.edu/comsa-dashboard/"

must_haves:
  truths:
    - "The deploy run triggered by the merge reports success for check-changes, build-and-push and deploy, read per job rather than from the overall run conclusion (ROADMAP criterion 1, D-12)"
    - "The step 'Verify backend package manifest (issue #123)' passes on the FIRST build, proving the committed manifest matches the image's /opt/package-manifest.csv exactly (ROADMAP criterion 1, D-02, D-12)"
    - "Both Stan models compile during that build and the deployed pod answers /api/health (ROADMAP criterion 2)"
    - "A job on frontend/public/sample_eava_child.csv (child, Mozambique, EAVA) on dev produces a real calibration with malaria calibrated and the R1 acceptance numbers unchanged (ROADMAP criterion 3, D-10, D-12)"
    - "A job on frontend/public/sample_insilicova_neonate.csv (neonate, Mozambique, InSilicoVA) on dev shows congenital malformation in the misclassification panel's 'Not calibrated, so absent from this matrix' notice and NOT in the 'Excluded from calibration (no observed deaths)' notice (ROADMAP criterion 4, D-07, D-12)"
    - "Both jobs' logs carry the line reading the vacalibration package version as 2.3.1 (D-08)"
    - "The backend-dependent suites are either executed against a real backend or their non-execution is recorded with the concrete reason and the substitute evidence (ROADMAP criterion 7, D-11, CLAUDE.md)"
  artifacts:
    - "No source artifact. This plan produces evidence only: run IDs, per-job conclusions, the manifest-diff step conclusion, two dev job IDs, and screenshots or quoted notice text."
  key_links:
    - "push to master -> check-changes paths filter ('backend/**' matched by the Dockerfile change) -> build-and-push -> the manifest diff -> deploy: if the paths filter does not match, the backend image is never built and the run still reports success (a trap recorded in phase 02-04)"
    - "the built image's /opt/package-manifest.csv <-> the committed backend/package-manifest.csv: disagreement fails the deploy, which is why plan 02.1-01 had to predict remotes 2.5.0 rather than discover it"
    - "vacalibration's donotcalib_tomodel -> not_calibrated_causes() -> MisclassificationMatrix.jsx notice: the disclosure route criterion 4 is checked on, with no frontend change in this phase"
  prohibitions:
    - statement: "No source file may be edited in this plan; a failure here routes back to plan 02.1-01 or 02.1-02"
      status: enforced
      verification: "git diff --quiet 30e082d2e6906d3c062f29fd5d2889fe463d90f2 -- . ':(exclude).planning' at the end of the plan"
    - statement: "Stored jobs computed on 2.2 must not be re-run or annotated (Deferred Ideas)"
      status: enforced
      verification: "only newly submitted jobs are inspected; no rerun endpoint is called on a historical job"
    - statement: "No test may be marked passed that did not run"
      status: enforced
      verification: "each suite in Task 3 is recorded with its actual command, exit code and reason if not run"
---

<objective>
Land the pin on master and prove it on the deployed stack: the image builds with the manifest diff
passing on the first attempt, the dev pod serves the new backend, and two real jobs demonstrate that
the issue #101 acceptance numbers are unchanged while a cause below 1 percent is disclosed as
declined rather than as a zero-count exclusion.

Purpose: everything before this is source-level. The forcing constraint of this phase — that the
image must keep building — is only actually proven by a build, and criterion 4's disclosure is only
actually proven in the UI a user reads.

Output: run IDs and per-job conclusions, the manifest-diff step result, two dev job IDs with quoted
notice text and log lines, and an explicit record for each backend-dependent suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-CONTEXT.md
@.planning/phases/02-reproducible-builds/02-04-SUMMARY.md
@.claude/skills/comsa-k8s-deploy/SKILL.md
@.claude/skills/test/SKILL.md
</context>

<verified_facts>
- `.github/workflows/deploy.yml` runs on push to master and on `workflow_dispatch`; `check-changes`
  filters on `backend/**` and `frontend/**`. This phase changes `backend/Dockerfile`, so the backend
  filter matches and the image is rebuilt.
- The manifest verification step is named "Verify backend package manifest (issue #123)", runs only
  when `build-backend` succeeded, pulls the image by digest, extracts `/opt/package-manifest.csv`,
  rejects an empty/short/mis-headed capture, and diffs against the committed file.
- The dev API is login-only since PR #112: only `/health`, `/auth/login` and `/auth/register` are
  public. Job submission and result inspection therefore run from a logged-in browser session.
- `kubectl` is not reachable from this machine; a pod restart is done by dispatching `deploy.yml`.
- Phase 02-04 recorded two traps worth repeating: read conclusions per job (`gh run view --json jobs`),
  never the overall run conclusion, and confirm the backend job actually ran rather than being
  filtered out.
</verified_facts>

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Land the pin on master and watch the build and deploy</name>
  <files>(no file changes — CI observation)</files>
  <read_first>
    - `.github/workflows/deploy.yml` lines 1-40 (triggers and the `check-changes` paths filter) and lines 118-160 (the manifest verification step)
    - `.planning/phases/02-reproducible-builds/02-04-SUMMARY.md` — how the previous phase read per-job conclusions and what it warns about
    - `.claude/skills/comsa-k8s-deploy/SKILL.md` — the deploy pipeline and how to dispatch it
  </read_first>
  <action>
    Ask the user to merge the phase commits to master following the repository's PR convention, then
    report the resulting `Deploy to k8s-dev` run. The agent cannot push, cannot merge, and cannot
    observe a run; everything after the merge in this task is scriptable and must be run.

    Once the run ID is known, read conclusions per job, confirm `build-and-push` actually ran (a
    backend-filtered-out run that reports success is the phase 02-04 trap), read the conclusion of the
    step named for the package manifest, and confirm the deployed health endpoint answers. If the
    manifest step fails, capture its diff output verbatim: it names exactly which rows the image
    produced that the committed file does not have, which routes straight back to plan 02.1-01's
    manifest edit.
  </action>
  <instructions>
    **What was built:** vacalibration pinned to GitHub commit `498df45b6e14e02a3152b843c605b4a33240f12e`,
    the manifest updated to `vacalibration,2.3.1`, a CI guard that rejects a
    floating ref, and the R-side adaptations to the widened learn rule.

    **What to do:**
    1. Merge the phase branch to master (PR per repo convention).
    2. Tell me the run ID of the `Deploy to k8s-dev` run it triggers, or just say "merged" and I will
       look up the latest run.

    **What this proves:** that the image still builds at all — which is the forcing constraint of this
    phase — and that the committed manifest matches the built image on the first attempt.

    **If the build fails:** paste the failing step's log. A failure in the vacalibration install step
    or in the manifest diff is this phase's bug, not flakiness, and goes back to plan 02.1-01.
  </instructions>
  <verification>
    After the merge, resolve the run ID (`gh run list --workflow deploy.yml --limit 1 --json databaseId,headSha`)
    and run the three commands in the verify block below: per-job conclusions, the package-manifest
    step's conclusion, and the deployed health endpoint.
  </verification>
  <resume-signal>Say "merged" (I will look up the run) or paste the run ID; paste the failing step's log if it went red.</resume-signal>
  <verify>
    <automated>gh run view "$RUN_ID" --json jobs --jq '.jobs[] | {name, conclusion}'</automated>
    <fails_when>any job reports a conclusion other than "success", or the list contains no build-and-push job (the backend paths filter did not match, so the new image was never built)</fails_when>
    <automated>gh run view "$RUN_ID" --json jobs --jq '.jobs[].steps[] | select(.name | test("package manifest")) | {name, conclusion}'</automated>
    <fails_when>the selection is empty (the manifest step did not run), or its conclusion is anything other than "success" — a "failure" here means the image's installed versions disagree with backend/package-manifest.csv</fails_when>
    <automated>curl -sS -o /dev/null -w '%{http_code}\n' https://dev.sites.idies.jhu.edu/comsa-dashboard/api/health</automated>
    <fails_when>the printed status code is not 200, or curl exits non-zero — the deployed pod is not serving the new backend</fails_when>
  </verify>
  <acceptance_criteria>
    - Every job in the run reports `success`, read per job.
    - A `build-and-push` job exists in that run.
    - The step matching "package manifest" reports `success` on the first build of this change.
    - The health endpoint returns HTTP 200.
  </acceptance_criteria>
  <done>
    The pinned image built on the amd64 runner, its package manifest matched the committed file with
    no diff, and the dev deployment is serving it.
  </done>
</task>
<task type="auto">
  <name>Task 2: Run the two acceptance jobs on dev and read the disclosures</name>
  <files>(no file changes — deployed verification)</files>
  <read_first>
    - `frontend/src/components/MisclassificationMatrix.jsx` lines 129-136 — the exact wording of the "Not calibrated, so absent from this matrix" notice and that it renders `notCalibrated`
    - `frontend/src/components/JobDetail.jsx` lines 345-371 — the "Excluded from calibration (no observed deaths)" notice, driven only by `results.zero_count_causes`
    - `.planning/REQUIREMENTS.md` section R1 — the malaria column-range numbers (0.1163 full matrix versus 0.0681 shrunk) that make "exclusion must not shrink the matrix" the acceptance rule
    - `.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-CONTEXT.md` decisions D-07 and D-12
  </read_first>
  <action>
    Two jobs, submitted through the logged-in dev UI (the API is login-only, so anonymous curl will
    return 401):

    Job A — `frontend/public/sample_eava_child.csv`, age group child, country Mozambique, algorithm
    EAVA. This is issue #101's sample; its smallest broad cause is 5.62 percent, so the widened learn
    rule excludes nothing extra. Confirm the results view shows a real calibration (values moved from
    uncalibrated), that malaria appears in the calibrated output and in the misclassification matrix,
    that injury and nn_causes appear under the zero-death notice, and that no stall warning is shown.

    Job B — `frontend/public/sample_insilicova_neonate.csv`, age group neonate, country Mozambique,
    algorithm InSilicoVA. Its broad `congenital_malformation` share is 1 of 1190 records
    (0.084 percent), which 2.3.1 declines under the sub-1 percent rule while 2.2 would have calibrated
    it. Confirm congenital malformation is named in the misclassification panel's not-calibrated
    notice, is absent from the zero-death notice, and still appears in the CSMF chart and the CSV
    export as an uncalibrated value rather than disappearing.

    For both jobs read the job log and quote the line carrying the vacalibration version; it must read
    2.3.1. Capture the job IDs and the quoted notice text for the SUMMARY.

    Report only what is on screen. If a notice reads wrong for the new rule, do not edit the frontend
    here — that is a finding that goes back to the user, because D-07 locked "no new UI wording" and
    changing it is a scope decision.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w '%{http_code}\n' https://dev.sites.idies.jhu.edu/comsa-dashboard/api/health</automated>
    <fails_when>the printed status code is not 200 — the deployment that the two acceptance jobs must run against is not serving</fails_when>
    <human-check>Job A results view: malaria present and calibrated; injury and nn_causes under the no-observed-deaths notice; calibrated values differ from uncalibrated; no stall warning.</human-check>
    <human-check>Job B results view: congenital malformation named in the "Not calibrated, so absent from this matrix" notice, absent from the no-observed-deaths notice, and still present in the CSMF chart and CSV export.</human-check>
    <human-check>Both job logs contain a line reporting the vacalibration package version as 2.3.1.</human-check>
  </verify>
  <acceptance_criteria>
    - Two job IDs recorded, one per dataset, both completed.
    - Job A: malaria calibrated; zero-death notice names exactly injury and nn_causes.
    - Job B: congenital malformation appears in the not-calibrated notice and nowhere in the
      zero-death notice.
    - Both logs quote a version line reading 2.3.1.
    - No file outside `.planning/` changed during this task.
  </acceptance_criteria>
  <done>
    The deployed stack demonstrates both halves of the behaviour change: unchanged acceptance on the
    issue #101 sample, and a truthful "declined to calibrate" disclosure for a cause below 1 percent.
  </done>
</task>
<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Decide how the backend-dependent suites are closed out</name>
  <decision>
    How to satisfy D-11 ("all suites run with zero skips") for the three suites that need a live
    plumber backend: `.claude/skills/test/scripts/test_backend.py`,
    `frontend/src/api/integration.test.js`, and the two Playwright specs in `frontend/e2e/`.
  </decision>
  <context>
    Before presenting this checkpoint, run `lsof -ti:8000` and, if nothing answers, attempt
    `cd backend && Rscript run.R &` exactly as CLAUDE.md instructs — without asking — and capture the
    real output. Quote that output here rather than paraphrasing it.

    Expected blocker on this machine: `backend/db/connection.R` needs DBI and RPostgres (both absent
    from the local R library), a PostgreSQL instance (none local; the deployed password is rotated and
    lives only in the k8s secret `comsa-db-credentials`) and Docker (not running). Both Playwright
    specs open with a `beforeAll` that calls `test.skip` when `http://localhost:8000/health` is
    unreachable, so running them blind produces a green report in which nothing executed — the exact
    silent pass CLAUDE.md and D-11 forbid. That is why this is a decision rather than an attempt.

    Whichever option is chosen, the SUMMARY must record for each of the three suites: the command
    attempted, the exit code or the concrete blocker, and the substitute evidence if any. A suite that
    did not run is written down as not run. Nothing here justifies editing a source file or weakening
    a spec's availability gate.
  </context>
  <options>
    <option id="provision-local">
      <name>Provision a local stack and run all three suites</name>
      <pros>Exercises the locally installed vacalibration 2.3.1 end to end through the HTTP layer, including the job worker and the database round-trip; satisfies D-11 literally.</pros>
      <cons>Requires starting Docker, running a PostgreSQL container, applying `backend/migrations/*.sql`, and installing DBI and RPostgres into the local R library — none of which this phase otherwise needs, and none of which is covered by the phase boundary.</cons>
    </option>
    <option id="accept-deployed">
      <name>Accept the deployed evidence from Task 2</name>
      <pros>Task 2's two jobs already drove the same user path — upload, submit, poll, read results and logs — through a real backend running the new image, which is stronger evidence for this phase's change than a local server would be; the Chrome E2E procedure in the test skill can add the click-through.</pros>
      <cons>Leaves the three suites unexecuted locally; the SUMMARY must say so plainly rather than reporting them green.</cons>
    </option>
  </options>
  <resume-signal>Select: provision-local or accept-deployed.</resume-signal>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> master -> GHCR image | The merge is what turns reviewed source into a running image |
| built image -> k8s-dev pod | The deploy job SSHes into the cluster with `secrets.K8S_SSH_PRIVATE_KEY` |
| browser session -> dev API | Login-only since PR #112; a JWT from the browser session is the only way in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-10 | Tampering | build reproducibility of the new image | high | mitigate | The manifest diff step must pass on the first build; its failure output names every drifted row, and a drift routes back to plan 02.1-01 rather than being waived. |
| T-02.1-11 | Repudiation | "the deploy succeeded" read from the wrong signal | medium | mitigate | Conclusions are read per job with `gh run view --json jobs`, and the presence of a `build-and-push` job is asserted, because the paths filter can let a run report success without building the backend. |
| T-02.1-12 | Spoofing | inspecting dev jobs without a session | low | mitigate | Job submission and inspection run inside the logged-in browser session; anonymous calls return 401 and would otherwise be misread as a broken deployment. |
| T-02.1-13 | Repudiation | a suite reported as passing that never executed | high | mitigate | Task 3 is a blocking human checkpoint: each backend-dependent suite gets a recorded disposition, and a Playwright run whose `beforeAll` skipped both specs may not be reported as a pass. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 02.1-03)

This plan creates no source artifact and changes no file outside `.planning/`. It produces evidence:
the deploy run ID and its per-job conclusions, the conclusion of the package-manifest verification
step, the `/api/health` status code, two dev job IDs with the quoted notice text and version log line
from each, and a recorded disposition for `test_backend.py`, `frontend/src/api/integration.test.js`
and the two Playwright specs.
</artifacts_produced>

<verification>
1. `gh run view <run-id> --json jobs` shows `success` for every job, including a `build-and-push` job.
2. The step matching "package manifest" shows `success` on the first build.
3. `curl https://dev.sites.idies.jhu.edu/comsa-dashboard/api/health` returns 200.
4. Job A on dev: malaria calibrated, injury and nn_causes under the zero-death notice.
5. Job B on dev: congenital malformation in the not-calibrated notice, absent from the zero-death
   notice, still present in the chart and export.
6. Both job logs quote a vacalibration version line reading 2.3.1.
7. Each backend-dependent suite has an explicit disposition in the SUMMARY.
</verification>

<success_criteria>
- ROADMAP criterion 1 (deployed half): the manifest diff passes on the first build.
- ROADMAP criterion 2 (deployed half): both Stan models compile in the image and the pod serves health.
- ROADMAP criterion 3 (deployed half): the issue #101 sample still calibrates with malaria calibrated.
- ROADMAP criterion 4 (deployed half): a 0.084 percent cause reads as declined, not as zero-count.
- ROADMAP criterion 7 (honest half): no suite is reported as passing that did not run.
- D-07 (UI half), D-11 (backend-dependent half) and D-12 are satisfied and evidenced.
</success_criteria>

<output>
Create `.planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-03-SUMMARY.md` when done. Record the run
ID and per-job conclusions, the manifest step conclusion (and its diff if it failed), the two dev job
IDs with the quoted notices and version log lines, and the per-suite disposition table from Task 3.
</output>