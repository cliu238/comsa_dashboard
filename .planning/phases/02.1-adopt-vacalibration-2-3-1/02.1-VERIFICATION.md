---
phase: 02.1-adopt-vacalibration-2-3-1
verified: 2026-09-13T04:16:49Z
status: passed
score: 7/7 must-haves verified
covered_files:
  - .planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-01-PLAN.md
  - .planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-01-SUMMARY.md
  - .planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-02-PLAN.md
  - .planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-02-SUMMARY.md
  - .planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-03-PLAN.md
  - .planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-03-SUMMARY.md
  - .planning/phases/02.1-adopt-vacalibration-2-3-1/02.1-CONTEXT.md
  - .planning/phases/02.1-adopt-vacalibration-2-3-1/deferred-items.md
  - backend/Dockerfile
  - backend/jobs/utils.R
  - backend/package-manifest.csv
  - tests/test_dockerfile_pinning.R
  - tests/test_vacalibration_backend.R
covered_digest: "v1:sha256:4add793e61067992d59c298530f2449cdeebdc39870c100f9718958dede02341"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
---

# Phase 02.1: Adopt vacalibration 2.3.1 Verification Report

**Phase Goal:** The backend runs vacalibration 2.3.1 (GitHub commit `498df45`) reproducibly on the existing 2026-08-01 CRAN snapshot, every test suite passes without skips, and the package's new sub-1% exclusion rule is disclosed to users the same way our own zero-count exclusion is.
**Verified:** 2026-09-13T04:16:49Z
**Status:** passed
**Re-verification:** No — initial verification

All checks below were run independently against the working tree at HEAD (`d665c85`, `docs/phase-02.1-close`, == `master` `7f35d7d` plus the plan-03 SUMMARY commit) rather than taken from SUMMARY.md narrative. Local R (4.5.2) already carries vacalibration 2.3.1, so every test command listed here was actually executed by the verifier in this session, not assumed.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria, cross-referenced against CONTEXT.md D-01…D-12)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `backend/Dockerfile` installs vacalibration from GitHub pinned to commit `498df45` (not a branch), `backend/package-manifest.csv` records `vacalibration,2.3.1`, and the `deploy.yml` manifest diff passes (D-01, D-02, D-12) | ✓ VERIFIED | `grep` confirms `ENV VACALIBRATION_SHA=498df45b6e14e02a3152b843c605b4a33240f12e` and the tarball install line `install.packages('https://github.com/sandy-pramanik/vacalibration/archive/${VACALIBRATION_SHA}.tar.gz', repos = NULL, type = 'source')` in `backend/Dockerfile`. `git diff --numstat 30e082d -- backend/package-manifest.csv` → `1  1` (only the `vacalibration` row changed, from `2.2` to `2.3.1`; `LaplacesDemon,16.1.8` was already present pre-phase, so no dependency drift was introduced). `Rscript tests/test_dockerfile_pinning.R` → re-run by the verifier, **40/40 passing**. Mutation-tested by the verifier: swapping the SHA for `mainline-branch-ref` makes the guard **exit 1** with 4 named FAILs, and the Dockerfile was restored byte-identical (`cmp -s`) afterward. Deploy evidence independently re-queried: `gh run view 34733552843 --json jobs` → `check-changes`/`build-and-push`/`deploy` all `success`; the step matching "package manifest" → `success`; `headSha` = `7f35d7dfd26c...` (the hotfix commit). The prior run `34733336669` on `eb3049e` is confirmed `failure` at `build-and-push` (deploy `skipped`), matching the documented "fixed forward by #133" narrative rather than contradicting it. |
| 2 | `CRAN_SNAPSHOT` is unchanged at 2026-08-01 and both Stan models compile at image build | ✓ VERIFIED | `git diff 30e082d -- backend/Dockerfile` shows zero changed lines inside `ENV CRAN_SNAPSHOT=...` or the Stan-compile `RUN` block; the only diff hunks are the new `VACALIBRATION_SHA` block and the install-line edits. Build log quoted in 02.1-03-SUMMARY (`seqcalib.stan` 1.1s, `seqcalib_mmat.stan` 1.0s) is corroborated by the independently re-confirmed `success` build-and-push job above. |
| 3 | Issue #101's sample file still produces a real calibration with `malaria` calibrated; R1 acceptance numbers unchanged | ✓ VERIFIED | Verifier re-ran the full R suite (real MCMC, no `--input-only`): **657/657 passing**, including `PASS: reliability rule: malaria's column range over all 9 child broad causes is 0.1163 (above threshold 0.1)` and `PASS: reliability rule: ... over only the 7 observed causes is 0.0681 (below threshold 0.1)` — the exact R1 numbers, unchanged. `git diff -U0 30e082d -- tests/test_vacalibration_backend.R \| grep -c 'repro:'` → `0`, confirming section 30b (the issue #101 reproduction, D-10) was not touched. Dev job A (`0acf3b48…`) independently corroborates on the deployed image: malaria uncalibrated 0.0843 → calibrated 0.0377; zero-death notice names exactly `injury, nn_causes`, matching `zero_count_causes(): eava entry is injury+nn_causes` asserted in the local suite. |
| 4 | A cause between 0–1% of deaths is disclosed as "declined to calibrate", not as a zero-count exclusion | ✓ VERIFIED | New section 30c (verifier re-ran it as part of the full-suite run above): fixture sanity confirms `congenital_malformation` = 1/1190 = 0.084%; `zero_count_causes()` does **not** name it; a real `vacalibration()` call reports it via `not_calibrated_causes()`; `Mmat_tomodel` stays a full `1x6x6` (not shrunk, satisfying REQUIREMENTS R1's "exclusion must not shrink the matrix" for this cause too); `assemble_calibration_result()` keeps it in `uncalibrated_csmf` and omits it from `zero_count_causes`. Frontend wording verified by reading `MisclassificationMatrix.jsx` line 131 (`Not calibrated, so absent from this matrix:`) and `JobDetail.jsx` lines 345-364 (the zero-death notice, driven only by `results.zero_count_causes`) — the two notices are structurally incapable of overlapping for this cause. Dev job B (`f013e6ec…`) corroborates: matrix footnote names `Congenital malformation, Other`; no zero-death notice rendered; CSV row present with a point-mass interval rather than absent. |
| 5 | Stall detection, zero-count display filtering, and `safe_cause_map()` are kept; the explicit zero-count `donotcalib` is either kept-with-comment or removed-with-tests, not left unexplained (D-06) | ✓ VERIFIED | `build_donotcalib()`'s comment block (read directly in `backend/jobs/utils.R` lines 763-776) states the 2.3.1 learn rule's 0.01/0.99 thresholds, that the two exclusion sets are unioned inside `vacalibration()`, and why the explicit list is kept (display filtering + the only logging point). The function body is byte-identical to pre-phase (only comment lines added — confirmed by reading the function; `zero_count_causes()`/`unobserved_causes()`/`safe_cause_map()` are all present and unchanged call sites). Section 30's `build_donotcalib()`/`zero_count_causes()` assertions (unchanged since pre-phase) still pass in the 657/657 run. |
| 6 | Section 12c's learn-rule flakiness (Phase 1 deferred item) is resolved, not skipped (D-09) | ✓ VERIFIED | Read section 12c directly: the expected set is now `setdiff(neonate_broad_causes, "other")` — a concrete, non-circular derivation (the initial `not_calibrated_causes(res90,...) == not_calibrated_causes(res90,...)` circularity flagged in `/code-review` was fixed in the post-review pass) — plus an independent assertion `not_calibrated_causes(res90, "interva", neonate_broad_causes)` equals exactly `"other"`. Both assertions passed in the verifier's own 657/657 run. This is deterministic (keyed off observed CSMF shares, not an MCMC draw), closing the flakiness by derivation rather than by a hardcoded/skipped number. |
| 7 | All test suites pass with zero skips | ✓ VERIFIED (with a properly-decided, evidenced exception for 3 backend-dependent suites) | Verifier independently re-ran every locally runnable suite: `tests/test_dockerfile_pinning.R` 40/40; `tests/test_vacalibration_backend.R` (full, real MCMC) 657/657; the four dependency-free suites 52+32+23+18 = 125/125; frontend unit suite (`npx vitest run --exclude '**/api/integration.test.js'`) 34 files / 365 tests, 0 failed, 0 skipped; `npm run build` succeeds. `frontend/src/api/integration.test.js`, `test_backend.py` and the two Playwright specs could not run on this machine (no DBI/RPostgres, no local Postgres) — this is a pre-existing environment limitation (documented in `~/.claude/projects/.../memory/local-r-env-cannot-run-backend.md`), not something this phase caused or hid. Plan 03's blocking `checkpoint:decision` (Task 3) recorded the attempt (`Rscript run.R` failing on missing `RPostgres`) and the user's explicit `accept-deployed` choice, with the two dev acceptance jobs (Task 2, same plan) as substitute evidence — those jobs exercise the identical upload→submit→poll→results→log→export path the unexecuted suites would exercise, against the real deployed image. This satisfies plan 03's own must-have wording ("either executed against a real backend or their non-execution is recorded with the concrete reason and the substitute evidence") and is not a silent skip: the two pre-existing, unrelated local failures (frontend lint's 4 errors, the integration-check's one endpoint mismatch) were independently re-confirmed by the verifier as present at the pre-phase commit `30e082d` too (`git diff --quiet 30e082d -- <implicated files>` → unchanged), so they are correctly out of this phase's scope and correctly logged in `deferred-items.md` rather than fixed or hidden. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/Dockerfile` | GitHub-pinned SHA install, snapshot/Stan-compile untouched | ✓ VERIFIED | Read directly; diffed against pre-phase commit; matches D-01/D-04/D-05 exactly |
| `backend/package-manifest.csv` | `vacalibration,2.3.1`, no other row changed | ✓ VERIFIED | `git diff --numstat` = `1 1`; only the vacalibration row |
| `tests/test_dockerfile_pinning.R` | New SHA-pin guard section, mutation-tested | ✓ VERIFIED | Re-run: 40/40; mutation test re-run by verifier: fails correctly, restores cleanly |
| `backend/jobs/utils.R` | `build_donotcalib()` comment, version log line | ✓ VERIFIED | Read directly at lines 735-806 |
| `tests/test_vacalibration_backend.R` | Sections 12c (fixed), 30 (version-line asserts), 30c (new) | ✓ VERIFIED | Read directly; re-run in the full 657/657 pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `backend/Dockerfile` SHA pin | `backend/package-manifest.csv` vacalibration row | `deploy.yml`'s "Verify backend package manifest" step | ✓ WIRED | Independently re-queried: step `success` on run `34733552843` (`headSha` = the hotfix commit) |
| `run_vacalibration()` / `run_pipeline()` | `prepare_calibration_exclusions()` | direct call | ✓ WIRED | `grep -n "prepare_calibration_exclusions"` shows both `backend/jobs/algorithms/vacalibration.R:202` and `backend/jobs/processor.R:178` calling the shared helper — one log line covers both paths |
| `vacalibration`'s `donotcalib_tomodel` | `MisclassificationMatrix.jsx` "Not calibrated" notice | `not_calibrated_causes()` → matrix payload | ✓ WIRED | Section 30c proves the function chain locally; dev job B's quoted footnote text matches the exact wording read from the component source |
| `zero_count_causes()` | `JobDetail.jsx` zero-death notice | `results.zero_count_causes` | ✓ WIRED | Notice is driven only by that field (read directly); section 30c proves `congenital_malformation` (a non-zero, sub-1% cause) is absent from it |

### Behavioral Spot-Checks / Suite Runs (executed by the verifier, not taken from SUMMARY)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Dockerfile pin guard | `Rscript tests/test_dockerfile_pinning.R` | 40/40 passed | ✓ PASS |
| Pin guard catches a branch-name ref | mutation: swap SHA → `mainline-branch-ref`, re-run, restore | exit 1, 4 named FAILs, byte-identical restore | ✓ PASS |
| Full R suite (real MCMC) | `Rscript tests/test_vacalibration_backend.R` | 657/657 passed | ✓ PASS |
| 4 dependency-free R suites | `Rscript tests/test_{misclass_matrix,auth_visibility,cors,input_persistence}.R` | 52+32+23+18 = 125/125 passed | ✓ PASS |
| Frontend unit suite | `npx vitest run --exclude '**/api/integration.test.js'` | 34 files / 365 tests, 0 failed, 0 skipped | ✓ PASS |
| Frontend build | `npm run build` | succeeds, 454 modules transformed | ✓ PASS |
| Frontend lint (pre-existing, out of scope) | `npm run lint` | 4 errors, unchanged since `30e082d` | ℹ️ INFO (confirmed pre-existing) |
| Integration check (pre-existing, out of scope) | `check_integration.py` | 1 endpoint mismatch, unchanged since `30e082d` | ℹ️ INFO (confirmed pre-existing) |
| Deploy run (independently re-queried) | `gh run view 34733552843 --json jobs` | all 3 jobs `success` | ✓ PASS |
| Manifest verification step (independently re-queried) | `gh run view 34733552843 --json jobs --jq '...package manifest...'` | `success` | ✓ PASS |
| Dev health endpoint (independently re-queried) | `curl .../api/health` | `200` | ✓ PASS |
| Dev job read-back (attempted) | `curl .../api/jobs/<id>` anonymously | `401 Missing or invalid Authorization header` | expected — confirms the documented login-only API behavior; job-level detail could not be independently re-fetched without a browser session, so Task 2's dev-job claims rest on the plan's own human-supervised checkpoint (see below) |

### Requirements Coverage (CONTEXT.md decisions D-01…D-12; REQUIREMENTS.md tracks only Phase 1's R1/R2/R3 and is not the ID source for this phase)

| Decision | Description | Status | Evidence |
|----------|-------------|--------|----------|
| D-01 | GitHub SHA pin, no dependency upgrade | ✓ SATISFIED | `repos = NULL` install; Imports pre-installed from snapshot; manifest diff = 1 line |
| D-02 | Manifest records `vacalibration,2.3.1`, no other drift | ✓ SATISFIED | `git diff --numstat` = `1 1` |
| D-03 | CI guard rejects a floating ref | ✓ SATISFIED | 40/40 passing test, mutation-verified by this verifier |
| D-04 | Stan compile step untouched | ✓ SATISFIED | diff shows zero changes to that block |
| D-05 | `knitr` still installed from snapshot | ✓ SATISFIED | present in install line and manifest; comment corrected to note it's a hard runtime dependency (verbose=TRUE call sites) |
| D-06 | Explicit `donotcalib` kept with redundancy comment | ✓ SATISFIED | comment read directly; body unchanged |
| D-07 | Sub-1% causes disclosed via existing notice, no new UI wording | ✓ SATISFIED | `git diff --stat 30e082d -- frontend/src` is empty; section 30c + dev job B prove the disclosure path |
| D-08 | Version logged once per job | ✓ SATISFIED | `add_log()` call read directly; asserted in section 30, exercised in dev job logs |
| D-09 | Section 12c fixed, not skipped | ✓ SATISFIED | non-circular derivation, passing |
| D-10 | Section 30b unchanged | ✓ SATISFIED | diff shows 0 lines touched carrying the `repro:` token |
| D-11 | Zero skips across all suites | ✓ SATISFIED | all locally-runnable suites re-run green; backend-dependent suites have a recorded, evidenced, user-decided disposition |
| D-12 | Deployed acceptance shape matches Phase 1's | ✓ SATISFIED | deploy run and manifest-diff step independently re-confirmed `success`; dev jobs recorded with quoted evidence |

No requirement IDs from REQUIREMENTS.md map to this phase (R1/R2/R3 are Phase 1's, correctly not claimed here); no orphaned requirements found for Phase 02.1.

### Regression Check Against Phase 2

| Phase 2 property | Status | Evidence |
|---|---|---|
| Base image digest pinned | ✓ held | `FROM rocker/r-ver:4.4@sha256:3dae5d2e...` unchanged in current `backend/Dockerfile` |
| `CRAN_SNAPSHOT` pinned to 2026-08-01 | ✓ held | unchanged, confirmed by diff |
| `sodium` installed explicitly | ✓ held | unchanged install line, unchanged manifest row |
| Manifest diff gate in `deploy.yml` | ✓ held and exercised | this phase's own deploy run is what re-proved the gate still works (and caught the first `repos = NULL` failure) |

### Anti-Patterns Found

None. `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across `backend/Dockerfile`, `backend/jobs/utils.R`, `tests/test_dockerfile_pinning.R`, and `tests/test_vacalibration_backend.R` returned no matches.

### Deferred Items (recorded in `deferred-items.md`, correctly out of this phase's scope)

| # | Item | Disposition |
|---|------|-------------|
| 1 | Frontend lint: 4 pre-existing errors | Confirmed unchanged since pre-phase commit `30e082d`; not this phase's files |
| 2 | Integration check: 1 pre-existing endpoint mismatch | Confirmed unchanged since pre-phase commit `30e082d`; not this phase's files |
| 3 | Sub-1% declined causes shown only in the matrix footnote, not the CSV/chart | Explicitly locked by D-07 ("no new UI wording" for this phase); raised by `/code-review`, logged as an open scope decision for the user, does not violate ROADMAP criterion 4 as scoped by D-07 |

None of these are gaps against this phase's own success criteria — they are correctly scoped exclusions, independently confirmed by this verifier (via `git diff --quiet` against the pre-phase commit) rather than taken on faith from the SUMMARY.

### Human Verification Required

None. The one item that inherently needs a browser session — the two dev acceptance jobs (Task 2 of plan 03) — was already executed under a human-supervised, non-autonomous checkpoint (`autonomous: false`, `checkpoint:human-action` / `checkpoint:decision` gates in the plan) during phase execution, not left for this verification pass. The quoted evidence (job IDs, exact log-line text matching `prepare_calibration_exclusions()`'s `add_log()` format, exact notice wording matching the frontend source read directly, and specific numeric CSMF/lambda values) is internally consistent with the codebase this verifier independently read, which is stronger corroboration than an isolated claim. The anonymous-401 check above confirms the stated login-only API behavior rather than contradicting the dev-job narrative.

### Gaps Summary

No gaps. All seven ROADMAP success criteria and all twelve CONTEXT.md decisions (D-01…D-12) are independently verified against the current codebase (HEAD `d665c85` / effectively `master` `7f35d7d`) by direct file inspection, live test-suite execution, and independent re-querying of the GitHub Actions run and the deployed health endpoint — not by trusting SUMMARY.md narrative. The three backend-dependent suites that did not execute locally have a properly recorded reason and substitute evidence, per the phase's own must-have wording, decided through a blocking human checkpoint rather than silently skipped.

---

*Verified: 2026-09-13T04:16:49Z*
*Verifier: Claude (gsd-verifier)*
