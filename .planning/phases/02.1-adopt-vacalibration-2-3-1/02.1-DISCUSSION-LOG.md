# Phase 02.1: Adopt vacalibration 2.3.1 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-12
**Phase:** 02.1-adopt-vacalibration-2-3-1
**Areas discussed:** Meeting scope, Version stamping. All other areas were settled from evidence gathered during the preceding `/gsd-explore` research and presented as forced conclusions the user could veto (none vetoed).

---

## Forced conclusions presented (not asked)

| Area | Conclusion | Forcing evidence |
|---|---|---|
| Install and pinning | GitHub commit `498df45`, snapshot unchanged, no dependency upgrades, guard test asserts a SHA | Phase 2 reproducibility rule; CRAN still 2.2; StanHeaders 2.39.1 breaks 2.2 |
| Keep explicit zero-count `donotcalib` | Keep with a redundancy comment | `zero_count_causes()` still drives UI hiding; explicit list is the only logged exclusion |
| UI wording | No new text | Sub-1 % exclusions already flow through `not_calibrated_causes()` into the matrix notice |
| Test 12c | Fix the expected set, do not skip | CLAUDE.md no-silent-skip rule; Phase 1 deferred item |
| knitr | Keep installing | Still Suggests-only in 2.3.1; runtime need observed on 2.2 |
| Acceptance | Real job on dev with the #101 sample and a 0-1 % dataset | Same shape as Phase 1 UAT |

---

## Meeting scope

| Option | Description | Selected |
|--------|-------------|----------|
| 没有其他内容 | Phase is a pure upgrade plus adaptation, per the 7 success criteria | ✓ |
| 有，我来描述 | User describes further meeting items; each triaged into this phase or Deferred | |

**User's choice:** 没有其他内容
**Notes:** Closes the open question from the exploration note.

---

## Version stamping

| Option | Description | Selected |
|--------|-------------|----------|
| 只写进 job 日志（推荐） | One job-log line with the package version; no UI change | ✓ |
| 日志加 UI 结果页都显示 | Log line plus the version on the results page | |
| 什么都不加 | No version record in this phase | |

**User's choice:** 只写进 job 日志（推荐）
**Notes:** Motivated by stored 2.2 jobs differing from 2.3.1 re-runs on datasets with a 0-1 % cause.

---

## Claude's Discretion

- Install mechanism (remotes vs. archive tarball) within the SHA-pin and no-upgrade constraints.
- SHA-recognition rule in the pinning guard test.
- Placement and wording of the version log line and the redundancy comment.
- Whether a local install helper is needed for the full R suite (keep out of the repo if trivial).

## Deferred Ideas

- Version shown in results UI or exports.
- Switch back to CRAN snapshot when 2.3.1 is on CRAN (seed exists).
- Retroactive handling of stored 2.2 jobs.
- Retiring `safe_cause_map()` dummies or using the package's broad-input `cause_map()` path.
- Adding the vacalibration R suite to CI.
