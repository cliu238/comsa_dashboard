---
phase: 01-issue-101-calibration-correctness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/jobs/utils.R
  - backend/jobs/algorithms/vacalibration.R
  - backend/jobs/processor.R
  - tests/test_vacalibration_backend.R
autonomous: true
requirements: [R3]

must_haves:
  truths:
    - "Post-vacalibration() result assembly exists in exactly one function; neither job path reimplements it."
    - "The R suite fails if either backend/jobs/algorithms/vacalibration.R or backend/jobs/processor.R stops calling that function."
    - "A vacalibration-only job and a pipeline job produce the same result-object field names and the same output CSVs as before this plan."
  artifacts:
    - path: "backend/jobs/utils.R"
      provides: "assemble_calibration_result() — the single post-calibration result assembler"
      contains: "assemble_calibration_result <- function"
    - path: "backend/jobs/algorithms/vacalibration.R"
      provides: "run_vacalibration() delegating assembly"
      contains: "assemble_calibration_result("
    - path: "backend/jobs/processor.R"
      provides: "run_pipeline() delegating assembly, adding only its pipeline-specific fields"
      contains: "assemble_calibration_result("
    - path: "tests/test_vacalibration_backend.R"
      provides: "Section 29 — behaviour tests for the shared assembler plus the both-paths-use-it guard"
      contains: "29. Shared result assembly"
  key_links:
    - from: "backend/jobs/algorithms/vacalibration.R"
      to: "assemble_calibration_result()"
      via: "direct call after vacalibration() returns"
      pattern: "assemble_calibration_result\\("
    - from: "backend/jobs/processor.R"
      to: "assemble_calibration_result()"
      via: "direct call after vacalibration() returns"
      pattern: "assemble_calibration_result\\("
    - from: "tests/test_vacalibration_backend.R"
      to: "both job path source files"
      via: "comment-stripped source assertions counting the de-duplicated primitives"
      pattern: "assemble_calibration_result\\("
---

<objective>
Collapse the two near-verbatim result-assembly blocks (`backend/jobs/algorithms/vacalibration.R:206-297` and `backend/jobs/processor.R:189-291`) into one shared function, and add a test that fails if either job path stops using it.

**This plan runs FIRST (wave 1) — an explicit ordering decision.** R1 (plan 01-01) and R2 (plan 01-02) both modify the code these two blocks duplicate. Fixing them before de-duplication means writing every fix twice, which is precisely the failure mode R3 exists to remove (per REQUIREMENTS.md R3: "Fixes for #101 landed in one path only, twice"). De-duplicating first means R1 and R2 are each implemented exactly once.

Purpose: remove the structural cause of one-sided fixes (R3) before the fixes land.
Output: `assemble_calibration_result()` in `backend/jobs/utils.R`, both callers rewritten to use it, and a guard test section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
</context>

<constraints>
- Simplest structure that removes the duplication. Do NOT introduce a class, an R6 object, a registry, or a config list. One function, two callers.
- Do NOT change any result-object field name, any output filename, any rounding, or any log message in this plan. This is a pure move-and-call refactor. R1 and R2 change behaviour; this plan must not.
- Do NOT delete `build_per_algorithm()`, `build_stall_fields()`, `extract_misclass_matrix()`, or `build_summary_df()`. They stay in `backend/jobs/utils.R` and are now called only by `assemble_calibration_result()`.
- `run_pipeline()`'s openVA loop and `run_vacalibration()`'s multi-file loading stay where they are. Only the post-`vacalibration()` assembly moves.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the failing tests for the shared assembler and the both-paths guard</name>
  <read_first>
    - tests/test_vacalibration_backend.R — read the harness header (lines 1-60: `test()`, `section()`, `backend_dir`), the `if (!input_only)` block boundaries (lines 803-810 open, line 1480 closes), and ALL of section 28 (lines 2125-2367). Section 28's final two `for (f in c(...))` loops (lines 2340-2367) currently assert that BOTH job paths call `build_summary_df()`, call `build_stall_fields()`, contain the literal `result_obj <- c(result_obj, stall_fields)`, and log `add_log(job$id, stall_fields$warning)`. Those four assertions become false by design in Task 2 and must be retargeted here.
    - backend/jobs/algorithms/vacalibration.R lines 186-297 — the assembly block being moved (source of truth for field names, rounding, filenames, log strings).
    - backend/jobs/processor.R lines 173-292 — the duplicate block, plus the pipeline-only fields that must NOT move (`n_records`, `openva_csmf`, `cause_counts`, `causes.csv`).
    - backend/jobs/utils.R lines 666-803 (`build_stall_fields`, `build_summary_df`) and lines 1041-1141 (`extract_misclass_matrix`, `build_per_algorithm`) — the primitives the new function will call.
    - tests/test_misclass_matrix.R lines 44-60 — the established pattern for sourcing `backend/jobs/utils.R` in a test without a database.
  </read_first>
  <files>tests/test_vacalibration_backend.R</files>
  <action>
Add a new `section("29. Shared result assembly (R3)")` AFTER section 28 and BEFORE the SUMMARY block at the end of the file. Section 29 must not require MCMC or a database, so place it outside the `if (!input_only)` block (i.e. after line 1480's closing brace, same as sections 13, 14 and 28).

`assemble_calibration_result()` calls `add_log()` and `add_job_file()`, which live in `backend/db/connection.R` and need Postgres. Do NOT skip the test and do NOT guard it behind a database check — define no-op stubs in the test file immediately before section 29 (`add_log <- function(...) invisible(NULL)` and `add_job_file <- function(...) invisible(NULL)`), and note in a comment that R's lexical scoping resolves these from the global environment so the sourced helper picks up the stubs. Use `tempfile()`/`dir.create()` for `output_dir` so nothing is written under `backend/data/`.

Reuse section 28's existing `fake_result(labels, lambda)` builder if it produces a usable `p_uncalib` / `pcalib_postsumm`; extend it locally (a new `fake_calib_result()` in section 29) if it does not carry `Mmat_tomodel`, `donotcalib_tomodel`, or `calibrated`.

Behaviour assertions for `assemble_calibration_result(calib_result, job, algo_names, output_dir, cause_display_names, cause_order)`:
  1. Single-algorithm result: the returned list has exactly these names present — `algorithm`, `age_group`, `country`, `ensemble`, `uncalibrated_csmf`, `calibrated_csmf`, `calibrated_ci_lower`, `calibrated_ci_upper`, `files` — plus the stall fields merged in (`path_correction_stalled` present).
  2. `algorithm` equals the `algo_names` vector that was passed in (all of them, not just the first).
  3. Ensemble result (labels include "ensemble"): the primary row used for `uncalibrated_csmf` / `calibrated_csmf` / both CI fields is the `ensemble` row, and `per_algorithm` is present with one entry per label.
  4. Single-algorithm result: `per_algorithm` is absent (matching `build_per_algorithm()` returning NULL for one label).
  5. All four CSMF/CI lists are rounded to 4 decimals.
  6. `calibration_summary.csv` is written into `output_dir` and `files$summary` equals the string `"calibration_summary.csv"`.
  7. Misclassification filenames: one algorithm gives `misclass_matrix.csv` and `files$misclass_{algo}`; two or more algorithms give `misclass_matrix_{algo}.csv` per algorithm. Assert the files exist on disk in `output_dir`.
  8. The written misclassification CSV's first column is named `CHAMPS_Cause` and its remaining column names are that algorithm's `va_causes`.
  9. `cause_display_names` and `cause_order` appear in the result only when non-NULL (a NULL argument must leave the field absent, not present-as-`{}` — assert with `!("cause_order" %in% names(res))` and with `!grepl("cause_order", jsonlite::toJSON(res, auto_unbox = TRUE))`).
  10. The stall-field `warning` is NOT present in the returned object (it is a log line, not payload) — assert `!("warning" %in% names(res))`.

Guard assertions (the R3 acceptance criterion "a test fails if either path stops using it"). Write a helper local to section 29 that reads a file, DROPS every line matching `^\s*#`, and pastes the rest — every source assertion below must run on that comment-stripped text, because both files contain comments that mention these identifiers and an unfiltered `grepl` would pass on prose alone. For each of `backend/jobs/algorithms/vacalibration.R` and `backend/jobs/processor.R`:
  - asserts `grepl("assemble_calibration_result(", stripped, fixed = TRUE)` is TRUE.
  - asserts each of these appears ZERO times in the stripped text: `build_per_algorithm(`, `build_stall_fields(`, `extract_misclass_matrix(`, `build_summary_df(`, `pcalib_postsumm[`, `p_uncalib[`, `result_obj <- c(result_obj, stall_fields)`, `CHAMPS_Cause`.
  - asserts `backend/jobs/utils.R` (comment-stripped) DOES contain each of `build_per_algorithm(`, `build_stall_fields(`, `extract_misclass_matrix(`, `build_summary_df(` and `add_log(job$id, stall_fields$warning)` — so the assertions cannot both pass by simply deleting the wiring everywhere.

Retarget section 28's two trailing `for (f in c(...))` loops (lines 2340-2367): delete the four assertions that require the two job paths to call `build_summary_df()` / `build_stall_fields()` / hold the inline `result_obj <- c(result_obj, stall_fields)` / log the warning, and replace them with the equivalent assertions against `backend/jobs/utils.R`. Keep the negative assertions that still hold (`no longer assembles calibrated_lower inline`, `no longer hardcodes LAMBDA_CEILING in a log line`) and run them comment-stripped.
  </action>
  <verify>
    <automated>Rscript tests/test_vacalibration_backend.R --input-only 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `Rscript tests/test_vacalibration_backend.R --input-only` exits non-zero and its failure list names the section 29 assertions (RED state — `assemble_calibration_result` does not exist yet). This is the expected state at the end of this task.
    - `grep -c 'section("29. Shared result assembly (R3)")' tests/test_vacalibration_backend.R` returns 1.
    - Section 29 appears after the line `} # end if (!input_only)` — verify with `grep -n 'end if (!input_only)\|29. Shared result assembly' tests/test_vacalibration_backend.R` showing the `end if` line number is smaller.
    - `grep -c 'add_job_file <- function' tests/test_vacalibration_backend.R` returns 1 (the stub exists; the test is not skipped).
    - No assertion in the file uses a bare `grepl(..., readLines(...))` without the comment-stripping helper for the new source assertions: `grep -n 'paste(readLines' tests/test_vacalibration_backend.R` shows every remaining occurrence is inside section 28's retained negative assertions or passes through the stripping helper.
    - `grep -c 'build_summary_df()' tests/test_vacalibration_backend.R` shows no assertion still requires `processor.R` or `vacalibration.R` to call it directly.
  </acceptance_criteria>
  <done>Section 29 exists, is RED, and section 28's now-contradictory source assertions have been retargeted at utils.R.</done>
</task>

<task type="auto">
  <name>Task 2: Extract assemble_calibration_result() and rewrite both call sites</name>
  <read_first>
    - backend/jobs/utils.R lines 666-803 and 1041-1141 — the primitives to call, and the file's comment style (each helper carries a short "why" comment; match it).
    - backend/jobs/algorithms/vacalibration.R lines 186-297 — the block to move verbatim in behaviour.
    - backend/jobs/processor.R lines 173-292 — the duplicate, and the pipeline-only fields to keep.
    - tests/test_vacalibration_backend.R section 29 (just written) — the contract the implementation must satisfy.
    - .planning/codebase/ARCHITECTURE.md, the "Duplicated result-assembly logic between vacalibration-only and pipeline paths" entry under Anti-Patterns — it prescribes this exact function boundary.
  </read_first>
  <files>backend/jobs/utils.R, backend/jobs/algorithms/vacalibration.R, backend/jobs/processor.R</files>
  <action>
Add to `backend/jobs/utils.R`, next to the other result-shaping helpers:

`assemble_calibration_result <- function(calib_result, job, algo_names, output_dir, cause_display_names = NULL, cause_order = NULL)`

It performs, in this order, exactly what the two blocks do today:
  1. `result_labels <- dimnames(calib_result$pcalib_postsumm)[[1]]`; `primary <- if ("ensemble" %in% result_labels) "ensemble" else result_labels[1]`.
  2. Build `uncalibrated`, `calibrated`, `calibrated_low`, `calibrated_high` as `as.list(round(..., 4))` from `p_uncalib[primary, ]` and `pcalib_postsumm[primary, c("postmean","lowcredI","upcredI"), ]`.
  3. `per_algorithm <- build_per_algorithm(calib_result)`.
  4. `stall_fields <- build_stall_fields(calib_result, primary)`; log via `add_log(job$id, stall_fields$warning)` when non-NULL, then set `stall_fields$warning <- NULL`.
  5. `misclass_matrix <- extract_misclass_matrix(calib_result, single_algo_name = if (length(algo_names) == 1) algo_names[1] else "combined")`.
  6. Write `calibration_summary.csv` into `output_dir` from `build_summary_df(...)` and register it with `add_job_file(job$id, "output", "calibration_summary.csv", ...)`.
  7. Write the misclassification CSV(s) with the existing naming rule (`misclass_matrix.csv` for one algorithm, `misclass_matrix_{algo}.csv` for several), first column `CHAMPS_Cause`, and register each.
  8. `add_log(job$id, "Results saved")`.
  9. Assemble and return `result_obj` with `algorithm = algo_names`, `age_group = job$age_group`, `country = job$country`, `ensemble = isTRUE(job$ensemble) || ("ensemble" %in% result_labels)` — read the two current call sites first and preserve whichever expression each uses; if they differ, pass the caller's already-computed `ensemble_val` in as an argument rather than recomputing. Merge the stall fields with `result_obj <- c(result_obj, stall_fields)`. Attach `cause_display_names` / `cause_order` only when non-NULL (conditional assignment, because `list(x = NULL)` keeps the element and jsonlite emits `{}` — this is already documented at `build_stall_fields()`). Attach `per_algorithm` and `misclassification_matrix` only when non-NULL, and add each misclassification filename under `result_obj$files[[paste0("misclass_", algo_name)]]`.

Rewrite `run_vacalibration()` (backend/jobs/algorithms/vacalibration.R): keep everything up to and including the `vacalibration()` call and `add_log(job$id, "Calibration complete")`, then replace lines 206-297 with a single call to `assemble_calibration_result(...)` and `return()` of its value. Delete the moved code — do not leave it commented out.

Rewrite `run_pipeline()` (backend/jobs/processor.R): keep the openVA loop, the `causes.csv` write and its `add_job_file()` call, and the `vacalibration()` call. Replace lines 189-291 with a call to `assemble_calibration_result(...)` followed by merging the pipeline-only fields: `n_records`, `openva_csmf`, `cause_counts`, and `files$causes <- "causes.csv"`. Merge so the shared fields are not overwritten (extend the returned list; do not rebuild it).

One deliberate behaviour unification, and the only one in this plan: `run_pipeline()` currently sets `algorithm = if (ensemble_val) sapply(algorithms, normalize_algo_name) else normalize_algo_name(algorithms[1])`, which drops the second algorithm on an independent multi-algorithm pipeline run (contradicting issue #83's per-algorithm reporting, which `run_vacalibration()` already does correctly). Pass the full normalized vector from both callers so `algorithm` always lists every algorithm. Add a section 29 assertion for this if Task 1 did not already cover it.
  </action>
  <verify>
    <automated>Rscript tests/test_vacalibration_backend.R --input-only 2>&1 | tail -10 && Rscript tests/test_misclass_matrix.R 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Rscript tests/test_vacalibration_backend.R --input-only` exits 0 with `Failed: 0`.
    - `Rscript tests/test_misclass_matrix.R` exits 0 with `Failed: 0`.
    - `Rscript tests/test_vacalibration_backend.R` (full, 2-5 min) exits 0 with `Failed: 0` and zero skips reported.
    - `Rscript -e 'source("backend/jobs/utils.R"); stopifnot(is.function(assemble_calibration_result))'` exits 0.
    - Comment-stripped duplication is gone: `grep -v '^[[:space:]]*#' backend/jobs/algorithms/vacalibration.R | grep -c 'build_summary_df(\|build_stall_fields(\|build_per_algorithm(\|extract_misclass_matrix(\|CHAMPS_Cause'` returns 0, and the same command against `backend/jobs/processor.R` returns 0.
    - `grep -v '^[[:space:]]*#' backend/jobs/algorithms/vacalibration.R | grep -c 'assemble_calibration_result('` returns 1, and the same for `backend/jobs/processor.R` returns 1.
    - Both files shrank: `wc -l backend/jobs/algorithms/vacalibration.R backend/jobs/processor.R` shows fewer lines than the pre-change 298 and 292.
    - No commented-out remnant of the moved block: `grep -c '^[[:space:]]*# *result_obj\|^[[:space:]]*# *summary_df' backend/jobs/algorithms/vacalibration.R backend/jobs/processor.R` returns 0 for both.
    - `python3 .claude/skills/test/scripts/check_integration.py --project-root .` exits 0.
  </acceptance_criteria>
  <done>One assembler in utils.R; both job paths call it; the full R suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → job record | `job$id`, `job$age_group`, `job$country` originate in a client request and are validated in `backend/plumber.R` before `save_job()` |
| job record → filesystem | the assembler writes CSVs under a directory derived from `job$id` and registers them in `job_files` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | `assemble_calibration_result()` output path | mitigate | The function takes `output_dir` as a parameter and must never build a path from a raw request field. Both callers keep computing it as `file.path("data","outputs", job$id)` where `job$id` is the server-generated UUID from `save_job()`; the function does not accept a filename from the caller either — `calibration_summary.csv` and `misclass_matrix*.csv` are literals inside it. |
| T-01-03-02 | Tampering | misclassification CSV filename built from `names(misclass_matrix)` | mitigate | Those names come from `dimnames(Mmat_tomodel)[[1]]` or the `algo_names` vector, both of which are constrained to the `require_algorithms()` enum in `backend/jobs/utils.R`; no user string reaches `paste0("misclass_matrix_", ...)`. Assert in review that `algo_names` is the normalized vector, not `job$algorithm` raw. |
| T-01-03-03 | Repudiation | job log completeness after the refactor | mitigate | The `add_log()` calls (`"Calibration complete"`, the stall warning, `"Results saved"`) must survive the move so a job's audit trail is unchanged; section 29 asserts `utils.R` contains `add_log(job$id, stall_fields$warning)`. |
| T-01-03-04 | Denial of Service | none new | accept | Refactor-only; no new input parsing, no new loops over user-sized data, no new external calls. |
</threat_model>

<verification>
1. `Rscript tests/test_vacalibration_backend.R` — full suite, `Failed: 0`, no skips.
2. `Rscript tests/test_misclass_matrix.R`, `Rscript tests/test_auth_visibility.R`, `Rscript tests/test_input_persistence.R` — all `Failed: 0`.
3. `cd frontend && npm test` — unchanged, still green (this plan does not touch the frontend; a failure here means a payload field name moved).
4. `lsof -ti:8000 || (cd backend && Rscript run.R &)` then `python3 .claude/skills/test/scripts/test_backend.py` — exits 0.
5. `python3 .claude/skills/test/scripts/check_integration.py --project-root .` — exits 0.
6. `cd frontend && npm run test:e2e` — exits 0 (proves a real job still completes and renders through the refactored path).
</verification>

<success_criteria>
- Addresses ROADMAP success criterion 6: both job paths provably share one result-assembly implementation, and a test fails if either stops using it.
- Zero behavioural change: result field names, output filenames, rounding and log messages are identical to pre-plan, except `algorithm` on an independent multi-algorithm pipeline run, which now lists every algorithm.
- All five test suites pass with zero skips (ROADMAP criterion 7).
</success_criteria>

<output>
After completion, create `.planning/phases/01-issue-101-calibration-correctness/01-03-SUMMARY.md`
</output>
