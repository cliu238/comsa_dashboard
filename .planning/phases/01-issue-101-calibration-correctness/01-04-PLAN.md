---
phase: 01-issue-101-calibration-correctness
plan: 04
type: execute
wave: 4
depends_on: ["01-02"]
files_modified:
  - frontend/src/components/CSMFChart.js
  - frontend/src/components/CSMFChart.test.js
  - frontend/src/components/JobDetail.jsx
  - frontend/src/components/JobDetail.test.js
  - frontend/src/components/MisclassificationMatrix.jsx
  - frontend/src/components/MisclassificationMatrix.test.js
autonomous: false
requirements: [R2]

must_haves:
  truths:
    - "A stalled run's credible intervals are drawn as whiskers in the CSMF chart and printed as lower-upper pairs in the comparison table."
    - "The stalled facet is labelled as the uncertainty of an uncalibrated estimate, stating that no calibration was applied — not as absent and not as unreliable."
    - "An ensemble whose constituent stalled shows its intervals, and the note about that constituent makes no claim about interval width or trustworthiness."
    - "No string in frontend/src calls an interval meaningless, unreliable, implausibly tight, falsely narrow or omitted."
    - "A point-mass interval (lower == upper) is still not drawn and still not printed — that suppression is a separate, still-correct rule."
  artifacts:
    - path: "frontend/src/components/CSMFChart.js"
      provides: "csmfWhisker() without the stall-suppression argument; buildCsmfFacets()/buildCsmfTableRows() without ciUnreliable"
      contains: "export function csmfWhisker(calibrated, ciLower, ciUpper)"
    - path: "frontend/src/components/JobDetail.jsx"
      provides: "relabelled stalled-facet note, factual stalled-constituent note, whiskers rendered for stalled facets"
      contains: "No calibration was applied"
    - path: "frontend/src/components/MisclassificationMatrix.jsx"
      provides: "pathCorrectionStalled prop replacing ciUnreliable"
      contains: "pathCorrectionStalled"
  key_links:
    - from: "frontend/src/components/JobDetail.jsx"
      to: "csmfWhisker()"
      via: "three-argument call, no stall flag"
      pattern: "csmfWhisker\\(calibrated, ciLower, ciUpper\\)"
    - from: "frontend/src/components/JobDetail.jsx"
      to: "MisclassificationMatrix"
      via: "pathCorrectionStalled prop derived from results.path_correction_stalled === true"
      pattern: "pathCorrectionStalled="
---

<objective>
Retract the false-precision framing on the frontend: draw a stalled run's credible intervals again in the chart and print them in the comparison table, relabel them as the uncertainty of an uncalibrated estimate, and delete every `ciUnreliable` read and every phrase that calls a statistically correct interval meaningless.

Purpose: R2's UI half. The chart currently suppresses the whiskers, the table prints `Calibrated (intervals omitted: unreliable)`, and the misclassification panel keys a note on `ciUnreliable`. All three rest on the wrong baseline comparison retracted in plan 01-02.
Output: `CSMFChart.js`, `JobDetail.jsx` and `MisclassificationMatrix.jsx` reading only `path_correction_stalled` / `stalled_constituents` / `lambda_calibpath`, with corrected wording and restored whiskers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-issue-101-calibration-correctness/01-02-SUMMARY.md
</context>

<retraction_scope>
Preserve:
- The `=== true` / `typeof === 'number'` strictness for every field crossing `unbox()` in `frontend/src/api/client.js`, and the both-shapes handling for `stalled_constituents` (a single stalled algorithm arrives as the bare string `"eava"`, two arrive as an array).
- Per-facet granularity: one algorithm can stall while another calibrates normally, so the note and the labels stay per facet, never global.
- The point-mass suppression in `csmfWhisker()` and in `buildCsmfTableRows()`'s `cell()` (`!(ciUpper > ciLower)`), decided on the RAW bounds rather than on rounded percents. That rule is about the causes vacalibration did not calibrate and is unrelated to the stall.
- The misclassification panel's issue-#116 caption: the matrix really is the identity-mixed prior, and its diagonal really is not empirical sensitivity. That statement is about the MATRIX, not about intervals, and stays.
- Backwards compatibility with older stored jobs, whose results still contain `ci_unreliable`. Nothing may read it; a missing field must never throw.

Remove:
- The fourth parameter of `csmfWhisker()` (currently `pathCorrectionStalled`, passed `facet.ciUnreliable` at the call site) and its early `return null`.
- `ciUnreliable` from the facet view-model and every read of `src.ci_unreliable`.
- The run-level `noCI` suppression in `buildCsmfTableRows()`.
- The row-type strings `Calibrated (intervals omitted: unreliable)` and `Calibrated (not calibrated: no usable path correction)`.
- The JobDetail note text `credible intervals are omitted`, `implausibly tight`, and `Credible intervals omitted`.
- The `ciUnreliable` prop on `MisclassificationMatrix`.
</retraction_scope>

<wording>
Exact strings, so the tests can assert them verbatim.

- Comparison-table row type, stalled facet: `Calibrated (none applied — interval is the uncalibrated estimate)`
- Comparison-table row type, every other case (including an ensemble with a stalled constituent): `Calibrated`
- Comparison-table row type, uncalibrated row: `Uncalibrated` (unchanged)
- Chart facet note, stalled facet: must contain `No calibration was applied`, the lambda rendered as `λ = 0.99` when known (fall back to `the identity ceiling` when it is not a number), and the sentence that the bars equal the uncalibrated estimate and the credible intervals shown are that estimate's own uncertainty (sampling error only). It must contain none of `omitted`, `unreliable`, `implausibly`, `not meaningful`, `falsely`.
- Chart facet note, ensemble with a stalled constituent: names the constituents via `formatAlgorithmName`, states that no calibration was applied to them, and states that the ensemble estimate is a genuine fit. It must make NO statement about the ensemble's interval width and must not contain `not calibrated`.
- Chart facet note, declined run (`calibration_declined` from plan 01-01, if that field reaches the facet): states that vacalibration could not calibrate this dataset and the values shown are uncalibrated.
- Misclassification panel note, stalled: keeps its current meaning — path correction could only use λ = <value>, so this matrix is close to the identity and the underlying CHAMPS estimate is unchanged and much further off-diagonal. Remove nothing else from it; it makes no interval claim.
- Chart legend: `95% CI` stays. It is now correct for a stalled facet too, because the interval genuinely is a 95% credible interval — of an uncalibrated estimate.
</wording>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert the frontend tests that encode the retracted framing</name>
  <read_first>
    - frontend/src/components/CSMFChart.test.js in full. The describes that must be inverted or rewritten: `path-correction stall flag (issue #101)` (~line 118), the whisker tests at ~lines 107-116 (`returns null when path correction stalled`), `table suppresses CI when path correction stalled (issue #101)` (~line 211), and `stalled estimate vs unreliable intervals (issue #101 follow-up)` (~line 256) including the `ci_unreliable` fixtures and the `stalled_constituents` shape tests at ~lines 280-315.
    - frontend/src/components/JobDetail.test.js lines 115-135 — `keys whisker suppression on ciUnreliable`, `renders a different notice when only the intervals are unreliable`, and `never tells the user an unreliable-CI row was not calibrated` (that last one splits the source on the literal `facet.ciUnreliable && (`, which stops existing).
    - frontend/src/components/MisclassificationMatrix.test.js lines 100-135 — the lambda/stall-note tests that pass `ciUnreliable`.
    - frontend/src/components/CSMFChart.js in full (145 lines) — the current view-model contract.
    - frontend/src/components/JobDetail.jsx lines 320-330 and 391-467 — the props passed to `MisclassificationMatrix` and the whole `CSMFChart` render function including the two note branches.
    - The `<retraction_scope>` and `<wording>` sections of this plan.
  </read_first>
  <behavior>
    - `csmfWhisker(0.30, 0.29, 0.31)` returns whisker offsets — there is no stall argument and no stall-based suppression.
    - `csmfWhisker(0.30, 0.30, 0.30)` returns null (point mass), and `csmfWhisker(0, 0.1, 0.2)` returns null (zero-height bar).
    - `buildCsmfFacets()` on a stalled payload exposes `pathCorrectionStalled: true`, `lambda: 0.99`, and NO `ciUnreliable` key.
    - `buildCsmfFacets()` on a payload with only the legacy `ci_unreliable: true` and no `path_correction_stalled` exposes `pathCorrectionStalled: false` (the legacy field is dead).
    - `buildCsmfTableRows()` on a stalled payload keeps `lower` and `upper` on every non-degenerate calibrated cell and sets the row type to the stalled string.
    - `buildCsmfTableRows()` on an ensemble facet with `stalled_constituents` keeps its intervals and uses the plain `Calibrated` row type.
    - A degenerate cell (`lower === upper`) still yields `lower: null, upper: null` on both a stalled and a healthy facet.
    - `stalledConstituents` still normalizes the bare-string and array shapes, and yields null for `{}`.
  </behavior>
  <files>frontend/src/components/CSMFChart.test.js, frontend/src/components/JobDetail.test.js, frontend/src/components/MisclassificationMatrix.test.js</files>
  <action>
Rewrite the affected describes to the behaviours above. Do not delete the scenarios — invert the expectations so a regression back to suppression fails. Update the block comments too: they currently explain that a stalled run's intervals are "~7x too tight", which is the claim being retracted.

Add these assertions:
  1. Whisker: three-argument signature. Assert `csmfWhisker.length === 3` so a fourth suppression parameter cannot creep back.
  2. Facet: `expect('ciUnreliable' in facet).toBe(false)` for stalled, ensemble and healthy payloads.
  3. Legacy payload: a fixture carrying only `ci_unreliable: true` produces `pathCorrectionStalled: false` and intervals that are kept — this is the older-job compatibility case and the proof the field is dead.
  4. Table: on a stalled payload, `groups[0].rows[1].cells` includes at least one cell with non-null `lower` and `upper`, and `groups[0].rows[1].type` equals the stalled string from `<wording>` exactly.
  5. Table: on an ensemble payload with `stalled_constituents: ['eava']`, the ensemble group's calibrated row type is exactly `Calibrated` and its intervals are kept.
  6. Retraction guard as a source assertion over the three component source files (`CSMFChart.js`, `JobDetail.jsx`, `MisclassificationMatrix.jsx`), read with `readFileSync` in the style JobDetail.test.js already uses, comments INCLUDED: zero case-insensitive occurrences of `ci_unreliable`, `ciUnreliable`, `implausibly`, `not meaningful`, `falsely`, `intervals omitted`, `7x`. One named test per pattern.
  7. JobDetail source assertions: the stalled note contains `No calibration was applied`; the stalled-constituent branch keys on `stalledConstituents` rather than on `ciUnreliable`; the stalled-constituent branch contains no `not calibrated` (keep the existing intent of that test, retargeted at the new branch); `csmfWhisker` is called with three arguments; `MisclassificationMatrix` receives `pathCorrectionStalled`.
  8. MisclassificationMatrix: the note renders when `pathCorrectionStalled` is true and the `ciUnreliable` prop no longer exists anywhere in the file.
  </action>
  <verify>
    <automated>cd frontend && npm test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test` exits non-zero, and the failures are the inverted expectations plus the retraction guard (RED).
    - `grep -c 'csmfWhisker.length' frontend/src/components/CSMFChart.test.js` returns 1.
    - `grep -c 'No calibration was applied' frontend/src/components/JobDetail.test.js` returns at least 1.
    - No test still asserts suppression: `grep -n 'returns null when path correction stalled\|drops lower/upper on the calibrated row when stalled\|keys whisker suppression on ciUnreliable' frontend/src/components/*.test.js` returns nothing.
    - Point-mass coverage survived: `grep -c 'point mass\|point-mass' frontend/src/components/CSMFChart.test.js` returns at least 1.
    - Zero tests were skipped or marked `.skip`: `grep -c '\.skip(' frontend/src/components/*.test.js` returns 0.
  </acceptance_criteria>
  <done>The frontend tests demand the corrected framing and are RED.</done>
</task>

<task type="auto">
  <name>Task 2: Restore the whiskers, relabel the notes, and delete every ciUnreliable read</name>
  <read_first>
    - The three test files as just rewritten — the contract.
    - frontend/src/components/CSMFChart.js in full — `buildCsmfFacets()` (lines 25-66), `csmfWhisker()` (lines 68-89), `buildCsmfTableRows()` (lines 99-145).
    - frontend/src/components/JobDetail.jsx lines 297-330 (summary block and the `MisclassificationMatrix` props, including the `zero_count_causes` disclosure added by plan 01-01) and lines 391-467 (the whole local `CSMFChart` render function: both note branches, the `csmfWhisker` call at line 439, the whisker `title` at line 443, and the legend at lines 460-464).
    - frontend/src/components/MisclassificationMatrix.jsx lines 166-209 — the component signature and the `ciUnreliable` note.
    - .planning/phases/01-issue-101-calibration-correctness/01-02-SUMMARY.md — the exact backend payload shape this consumes.
    - .planning/phases/01-issue-101-calibration-correctness/01-01-SUMMARY.md — whether plan 01-01 put `calibration_declined` in the per-row payload, which this task's `buildCsmfFacets()` change depends on. Do not assume; read it.
    - The `<retraction_scope>` and `<wording>` sections of this plan.
  </read_first>
  <files>frontend/src/components/CSMFChart.js, frontend/src/components/JobDetail.jsx, frontend/src/components/MisclassificationMatrix.jsx</files>
  <action>
`CSMFChart.js`:
  - `csmfWhisker(calibrated, ciLower, ciUpper)` — drop the fourth parameter and the `if (pathCorrectionStalled) return null` line. Keep the null-bounds guard, the zero-`calibrated` guard, and the `!(ciUpper > ciLower)` point-mass guard. Rewrite the doc comment: the interval of a stalled run is the uncertainty of the uncalibrated estimate and is drawn like any other; only a point mass and a zero-height bar are skipped.
  - `buildCsmfFacets()` — delete the `ciUnreliable` property and the `src.ci_unreliable` read. Keep `lambda`, `pathCorrectionStalled`, `stalledConstituents` and the causes array unchanged. Surface `calibrationDeclined` from `src.calibration_declined === true` if plan 01-01 put that field in the per-row payload; check `01-01-SUMMARY.md` first rather than assuming. Rewrite the block comment that describes the two flags.
  - `buildCsmfTableRows()` — delete the `noCI` variable and its use in `cell()`; a cell's bounds are now dropped only when `degenerate`. Reduce the row `type` to two states: the stalled string from `<wording>` when `src.path_correction_stalled === true`, otherwise `Calibrated`. Rewrite the comment that explains the suppression.

`JobDetail.jsx`:
  - Replace the two note branches. The stalled branch renders the stalled wording from `<wording>` (lambda when numeric, else the identity-ceiling fallback). Replace the `facet.ciUnreliable && (...)` branch with one keyed on `facet.stalledConstituents?.length`, rendering the ensemble wording — no interval-width claim. If `calibrationDeclined` reached the facet, render its note too.
  - Call `csmfWhisker(calibrated, ciLower, ciUpper)` with three arguments. Keep the `95% CI: [...]` title on the whisker and keep the legend entry.
  - Change the `MisclassificationMatrix` props: replace `ciUnreliable={results.ci_unreliable === true || results.path_correction_stalled === true}` with `pathCorrectionStalled={results.path_correction_stalled === true}`. Leave the `lambda` prop as it is.

`MisclassificationMatrix.jsx`:
  - Rename the `ciUnreliable` prop to `pathCorrectionStalled` in the signature and at its single use. Keep the note's text (it describes the matrix, not the intervals) and keep the issue-#116 caption and comment block.

Change no CSS and add no new component file. Do not reintroduce suppression behind a flag or a prop.
  </action>
  <verify>
    <automated>cd frontend && npm test 2>&1 | tail -12 && npm run lint 2>&1 | tail -4 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test` exits 0 with zero failures and zero skips.
    - `cd frontend && npm run lint` exits 0 and `cd frontend && npm run build` exits 0.
    - `grep -ric 'ci_unreliable\|ciUnreliable' frontend/src` returns 0.
    - `grep -ric 'implausibly\|not meaningful\|falsely narrow\|intervals omitted' frontend/src` returns 0.
    - `grep -c 'export function csmfWhisker(calibrated, ciLower, ciUpper)' frontend/src/components/CSMFChart.js` returns 1.
    - `grep -c 'csmfWhisker(calibrated, ciLower, ciUpper)' frontend/src/components/JobDetail.jsx` returns 1.
    - `grep -c 'pathCorrectionStalled=' frontend/src/components/JobDetail.jsx` returns 1 and `grep -ric 'ciUnreliable' frontend/src/components/MisclassificationMatrix.jsx` returns 0.
    - `grep -c "No calibration was applied" frontend/src/components/JobDetail.jsx` returns 1.
    - Point-mass suppression intact: `grep -c 'ciUpper > ciLower' frontend/src/components/CSMFChart.js` returns 1 and `grep -c 'rawHi > rawLo' frontend/src/components/CSMFChart.js` returns 1.
    - No new CSS or component file: `git status --porcelain frontend/src` lists only the six files in this plan's `files_modified`.
  </acceptance_criteria>
  <done>The chart draws a stalled run's whiskers, the table prints its bounds, every surface labels them as an uncalibrated estimate's uncertainty, and no `ciUnreliable` read remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the retracted framing is gone from a real job's results view</name>
  <read_first>
    - .planning/ROADMAP.md Phase 1 success criteria 1-5 — the five user-visible statements being confirmed.
    - frontend/public/sample_eava_child.csv — the file to upload (2383 child records; `injury` and `nn_causes` have zero deaths).
  </read_first>
  <files>none — verification only, no file is modified by this task</files>
  <action>
Run the automation before pausing, then pause for the human. Start the backend if it is not up (`lsof -ti:8000 || (cd backend && Rscript run.R &)`), start the Vite dev server (`cd frontend && npm run dev`), and submit a vacalibration job through the UI with `frontend/public/sample_eava_child.csv`, age group child, country Mozambique, algorithm EAVA, "Combine algorithms?" off. Poll until the job status is `completed`, record the job id and the λ value from the job log, then present the checklist below and wait. Do NOT self-approve and do NOT proceed past this task without the human's response.
  </action>
  <verify>
    <automated>lsof -ti:8000 || (cd backend && Rscript run.R &) ; sleep 5 ; curl -s localhost:8000/health</automated>
  </verify>
  <what-built>
Every automated check in this phase already passes: the full R suite, the frontend unit suite, lint, build, the Python API scripts, the integration check and Playwright. This checkpoint confirms the statistical presentation reads correctly to a human, which no assertion can prove.

Before pausing, run the automation: start the backend if needed (`lsof -ti:8000 || (cd backend && Rscript run.R &)`), start the frontend (`cd frontend && npm run dev`), and submit a vacalibration job with `frontend/public/sample_eava_child.csv`, age group child, country Mozambique, algorithm EAVA, "Combine algorithms?" off. Wait for it to complete and note the job id and the λ reported in the log.
  </what-built>
  <how-to-verify>
1. Open the job's Results tab.
2. Summary block: it names `Injury` and `Neonatal Causes` as excluded for having no observed deaths.
3. CSMF chart: exactly 7 cause groups per facet — no `Injury`, no `Neonatal Causes`. The calibrated bars differ visibly from the uncalibrated ones (`Other Infections` should be clearly taller after calibration). Whisker marks are drawn inside the calibrated bars.
4. If (and only if) the facet carries a stall note, read it: it must say no calibration was applied and describe the intervals as the uncalibrated estimate's own uncertainty. It must not say "omitted", "unreliable", "implausibly tight" or "not meaningful". With the fix in place this file should NOT stall — if you see a stall note here, that is a defect, not a wording question.
5. CSMF Comparison table: 7 cause columns; the Calibrated row prints `NN% (lo–hi)` pairs; its Type cell reads `Calibrated`.
6. Misclassification panel: no `Injury` or `Neonatal Causes` row, column or footnote. The "Not calibrated, so absent from this matrix" footnote, if present, names only causes with observed deaths (`Other` is the usual one).
7. Download `calibration_summary.csv` from the job's files: 7 data rows, `calibrated_lower`/`calibrated_upper` populated, `lambda_calibpath` recorded.
8. Export the comparison table as CSV: no zero-death cause column.
  </how-to-verify>
  <acceptance_criteria>
    - The human confirms items 2-8, or reports which one failed with the exact text seen.
    - No screen shows `Injury` or `Neonatal Causes`.
    - No screen shows the words "omitted", "unreliable", "implausibly" or "not meaningful" in connection with an interval.
    - `calibration_summary.csv` has populated bound columns.
  </acceptance_criteria>
  <resume-signal>Type "approved", or paste the exact text of anything that reads wrong.</resume-signal>
  <done>A human has confirmed the results view on the issue's own file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> React state | passes through `unbox()` in `frontend/src/api/client.js`; scalars may arrive as length-1 arrays, `NULL` as `{}`, `NA` as the string `"NA"` |
| result payload -> rendered DOM | cause names and algorithm names become text nodes and `title` attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | a malformed or hostile `path_correction_stalled` / `stalled_constituents` value driving the label | mitigate | Keep the strict `=== true` and `typeof === 'number'` checks and the `Array.isArray(...) ? ... : typeof ... === 'string' ? [...] : null` normalization. A non-boolean must read as "unknown" and render no note, never crash the Results tab. The existing `{}`-shaped and `"NA"`-shaped fixtures in CSMFChart.test.js cover this and must keep passing. |
| T-01-04-02 | Spoofing/Injection | constituent and cause names interpolated into the notes | accept | React escapes text children; the notes use template interpolation, not `dangerouslySetInnerHTML`. Names are constrained server-side to the algorithm enum and the fixed broad-cause vocabulary. |
| T-01-04-03 | Information disclosure | drawing intervals that were previously hidden | accept | The bounds were always in the payload; only the render suppressed them. Nothing new crosses the boundary. |
| T-01-04-04 | Denial of Service | division by `calibrated` in the whisker geometry | mitigate | `csmfWhisker()` keeps its `!calibrated` guard, so a zero-height bar returns null instead of producing `Infinity`/`NaN` CSS percentages. Assert it in the tests. |
</threat_model>

<verification>
1. `cd frontend && npm test` — zero failures, zero skips, total count at least the pre-plan 315 (measured 2026-08-24: 34 files, 315 passing).
2. `cd frontend && npm run lint && npm run build` — both exit 0.
3. `Rscript tests/test_vacalibration_backend.R` — full suite, `Failed: 0` (this plan does not touch the backend; a failure means a payload contract drifted).
4. `Rscript tests/test_misclass_matrix.R`, `Rscript tests/test_auth_visibility.R`, `Rscript tests/test_input_persistence.R` — all `Failed: 0`.
5. `python3 .claude/skills/test/scripts/check_integration.py --project-root .` — exits 0.
6. `lsof -ti:8000 || (cd backend && Rscript run.R &)` then `python3 .claude/skills/test/scripts/test_backend.py` — exits 0.
7. `cd frontend && npm run test:e2e` — exits 0 (both `demo-gallery.spec.js` and `file-upload.spec.js`).
8. Phase-wide retraction gate, comments included: `grep -ric 'ci_unreliable\|ciUnreliable\|implausibly\|not meaningful\|falsely narrow\|intervals omitted' backend/ frontend/src/ frontend/e2e/ tests/` returns 0.
</verification>

<success_criteria>
- ROADMAP criterion 4: credible intervals are shown for a stalled run and labelled as the uncertainty of an uncalibrated estimate rather than as unreliable or absent.
- REQUIREMENTS R2: intervals restored in the chart and the comparison table; lambda still reported; stall detection unchanged; no wording anywhere claims the intervals are meaningless or falsely narrow.
- ROADMAP criterion 7: all five test suites pass with zero skips.
</success_criteria>

<output>
After completion, create `.planning/phases/01-issue-101-calibration-correctness/01-04-SUMMARY.md`
</output>
